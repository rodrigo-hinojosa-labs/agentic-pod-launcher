# Coverage Map 011-019 (support artifact)

| Subsystem | Feat | Documented? | Best home | Behavior | Code |
|-----------|------|-------------|-----------|----------|------|
| Deployment mode selection (docker\|local) | 011 | partial — getting-started.md (§Local standalone mode) and architecture.md (§Deployment modes) cover it, but README.md never mentions local mode and docs/agentic-quickstart.{en,es}.md's wizard variable list has no deployment-mode variable at all (its Deployment section only has DESTINATION/INSTALL_SERVICE) | getting-started | The wizard's first prompt asks for a deployment mode; agent.yml gains deployment.mode as single source of truth (enum docker\|local, legacy backfilled to docker on --regenerate). Local scaffolds skip all Docker artifacts; a mode switch on --regenerate warns about orphaned artifacts and never deletes. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/setup.sh:451 (ask_choice "Deployment mode") + scripts/lib/schema.sh |
| Local Remote Control systemd session + one-time --login | 011 | yes — getting-started.md:40-66 covers the flow, trade-off and manual verification gate (deep detail delegated to specs/011 quickstart); architecture.md carries the Principle II trade-off note | getting-started | In local mode the agent persists as a claude remote-control session under a system systemd unit (Restart=always, ExecCondition on credentials, never --dangerously-skip-permissions). ./setup.sh --login verifies Claude Code >= 2.1.51, runs the OAuth login, applies workspace trust, pre-accepts the Remote Control prompt, and installs/enables the staged units. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/systemd-remote-control.service.tpl + setup.sh --login flow + scripts/lib/local_trust.sh |
| Local healthcheck timer (alive/connected/expired) | 011 | partial — one sentence at getting-started.md:66; the three states, notify config, and journal commands are not referenced anywhere in docs/ | getting-started | A ~5-min systemd timer distinguishes session alive vs connected (live :443 socket owned by the session PID) vs expired auth, degrades gracefully without jq/ss, and can notify with the token kept off argv. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-healthcheck.sh.tpl (+ .service/.timer tpl) |
| Local MCP runtime bootstrap (agent-bootstrap.sh) | 011 | no — "agent-bootstrap" appears in no doc; adding-an-mcp.md also never mentions local-mode runtime provisioning | getting-started | Local mode provisions exactly the runtimes .mcp.json references — uv/uvx, node/npx symlinks, github-mcp-server (checksum-verified), bun — into ~/.local/bin, idempotent and best-effort, run by --login. Without it every project MCP fails to connect under systemd's minimal PATH. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-bootstrap.sh.tpl |
| agentctl mode-awareness (local status/doctor, docker-only degradation) | 011 | partial — one line at getting-started.md:63; no doc lists which subcommands degrade or what local status/doctor report | getting-started | In local mode, docker-only agentctl subcommands error with a systemctl/journalctl hint instead of touching docker; status/doctor read systemd unit states, the on-disk login, qmd/backup/wiki-graph state files and index presence (012/014 extended this). | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/agentctl:1235 (cmd_local_heartbeat/status/doctor dispatch, unit reporting at :976-1011) |
| Local kill switch (session + all companion units) | 011 | partial — next-steps.en.tpl:304 documents the invocation with its full scope; getting-started/architecture mention the kill switch only in passing | getting-started | scripts/local/agent-killswitch.sh stops the session plus every companion unit — qmd-reindex timer, qmd-watch, wiki-graph timer, vault-backup, healthcheck (scope expanded by 013 US3 and 014). Before 013 it kept pushing backups while "killed". | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-killswitch.sh.tpl:17-19 (AUX_UNITS) |
| Mode-resolved vault seeding + MCP paths (VAULT_MCP_PATH, GCAL_CREDS_PATH) | 012 | partial — architecture.md:283 covers it fully; vault.md's path table has a local column but its seeding narrative is still docker-phrased | vault | Local mode seeds the vault skeleton host-side at scaffold/regenerate under <workspace>/<vault.path> (no /home/agent rebase), and .mcp.json's vault and google-calendar paths are precomputed per mode (docker stays byte-identical). | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/mcp-json.tpl:34,70 ({{GCAL_CREDS_PATH}}, {{VAULT_MCP_PATH}}) + setup.sh _seed_vault_local |
| QMD RAG pipeline as systemd units (reindex timer + inotify watcher + OnCalendar conversion) | 012 | partial — architecture.md:283/285 is thorough; vault.md's QMD operations section ("QMD is not auto-configured at boot", manual bunx commands at :324-335) predates even feature 010 and contradicts the shipped behavior | vault | Local mode runs the reindex as a service+timer (cron schedule converted via local_schedule.sh cron_to_systemd_calendar, unsupported forms fall back with a consultable qmd-schedule.fallback marker) and an inotify watcher unit with a supervised loop + real vault env (013 RC2/RC3 hardening: wrappers self-provide PATH first). Self-heals via setup-if-needed on every tick. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-qmd-reindex.sh.tpl + modules/local-qmd-watch.sh.tpl:36-38 + scripts/lib/local_schedule.sh |
| Local vault backup timer | 012 | partial — architecture.md:283 gives one sentence; vault.md never mentions the local backup timer | vault | Vault backup to the fork's backup/vault orphan branch runs as a systemd service+timer in local mode over the same backup_vault.sh (hash idempotency, VAULT_ROOT_OVERRIDE, no-op without a fork), closing the backup/restore asymmetry. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-vault-backup.sh.tpl (+ .service/.timer tpl, scripts/lib/backup_vault.sh) |
| QMD storage env contract (XDG_CACHE_HOME + QMD_CONFIG_DIR under .state/.cache) | 013 | partial — architecture.md:285 explains the contract; state-layout.md only lists generic .state/.cache\|.config XDG rows with no qmd-specific entries (no index.sqlite, no .config/qmd) | state-layout | The qmd binary honors XDG_CACHE_HOME (index+models) and QMD_CONFIG_DIR (collections) — NOT QMD_CACHE_HOME, which only the bash lib reads — so local mode exports all three converging on <ws>/.state/.cache/qmd, and the qmd MCP env in .mcp.json is a precomputed {{QMD_MCP_ENV}} pin (writer+reader atomic pair; index survives workspace migration). | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-qmd-reindex.sh.tpl:32-46 |
| agentctl doctor 0/1/2 exit-code contract + RAG freshness | 013 | partial — architecture.md prose mentions the 0/1/2 degradation for 013 and 014; no reference doc enumerates the exit-code contract or the thresholds (DOCTOR_*_MAX_AGE envs) | heartbeatctl | doctor degrades honestly with exit codes 0/1/2 on reindex last_status=error, backup staleness (>25h), failed watcher, and (014) wiki-graph integrity findings/dead runner; status shows last-run freshness and wiki-graph counts. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/agentctl:105-120 (_doctor_warn/_doctor_fail counters) + :976-1011 |
| Manual RAG entrypoints: agentctl heartbeat qmd-reindex\|backup-vault\|wiki-graph | 013 | partial — heartbeatctl.md:327-336 documents wiki-graph, but the qmd-reindex and backup-vault subcommands (010/013) are absent from the heartbeatctl subcommand reference entirely (zero 'qmd' hits in that doc) | heartbeatctl | Operators can force a reindex, vault backup, or wiki-graph derivation on demand in BOTH modes via agentctl heartbeat <sub> (local runs the rendered entrypoints; reindex refuses --dry-run, backup passes it through). wiki-graph added by 014. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/agentctl (cmd_local_heartbeat) + docker/scripts/heartbeatctl:127-130 |
| Wiki-graph: derived knowledge graph + structural lint | 014 | yes — vault.md:177-203, architecture.md:287, heartbeatctl.md:327 | vault | A deterministic (LLM-free, never-editing) runner derives <vault>/.graph/{graph,backlinks,findings}.json from the whole wiki — findings include orphans, broken wikilinks, frontmatter violations, index.md drift, stale pages, alias occurrences — scheduled 20 */6 by default in both modes, state in wiki-graph.json. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/wiki_graph.sh |
| Normalization layer (wiki/normalization/) | 014 | yes — vault.md:182-190 and architecture.md:287 | vault | The vault skeleton gains wiki/normalization/ declaring canonical forms + aliases (e.g. Cencosud <- SENCOSUD) outside the six knowledge types; the schema's ingest step normalizes terminology and the query step becomes graph-aware (1-hop neighbors from backlinks.json); the linter reports alias occurrences informationally. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/vault-skeleton/wiki/normalization |
| Additive vault upgrade (vault_seed_missing) | 014 | yes — vault.md:203 (auto-added on next boot/regenerate) + architecture.md:287 (marker mechanics) | vault | Existing populated vaults receive only the NEW structures on the next boot/regenerate — never overwritten, the co-evolved vault CLAUDE.md untouched — with the schema delta gated by a hidden .applied marker so it is never re-deposited after the operator deletes the delta doc. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/vault.sh:70 (vault_seed_missing) + modules/vault-deltas/schema-updates-0.8.0.md |
| Absolute claude CLI resolution for local units | 015 | no — 'claude_cli' and 'resolve_claude_bin' appear in no doc | getting-started | detect_claude_cli/resolve_claude_bin persist an absolute claude path into agent.yml (claude_cli) and unit emission re-resolves against the operator HOME, failing LOUD with an actionable message instead of emitting a bare 'claude' ExecStart that systemd cannot resolve (status=203/EXEC restart loop). | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/setup.sh:89-137 (resolve_claude_bin, detect_claude_cli, _persist_claude_cli; re-resolve at :2339) |
| Host-libc-aware bun provisioning (_libc_variant) | 015 | no — '_libc_variant' / musl-vs-glibc bun selection appears in no doc | getting-started | The local bootstrap probes the host libc (musl loader -> ldd -> getconf -> default glibc) and downloads the matching bun build; the idempotency guard tests actual execution (bun --version), so a wrong-libc binary is re-provisioned instead of skipped as present. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/modules/local-bootstrap.sh.tpl:149 (_libc_variant) |
| RAG observability + host-backed scratch (rag_obs.sh) | 015 | no — 'rag_obs', 'scratch_dir', 'redact_secrets', TMPDIR routing appear in no doc | architecture | qmd reindex and wiki-graph route heavy temporaries (TMPDIR) to disk-backed <ws>/.state instead of the 100MB /tmp tmpfs (no more ENOSPC on large vaults), and their real stderr + effective env are captured into logs/state files — always passed through redact_secrets so credentials never reach a log. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/rag_obs.sh (redact_secrets, scratch_dir; consumed by qmd_index.sh + wiki_graph.sh) |
| Managed qmd bun-install prefix (bunx retired for qmd) | 016 | partial and STALE — qmd-upgrade-checklist.md explains the prefix strategy, but architecture.md:279 still says qmd is 'invoked via bunx' and vault.md:318-320 still carries the 'Why bunx and not npx' rationale — both contradict shipped 016 behavior | vault | qmd no longer runs via bunx: _qmd_ensure_prefix installs the pinned package into $QMD_CACHE_HOME/pkg with trustedDependencies limited to better-sqlite3 + node-llama-cpp (tree-sitter stays WASM/unbuilt), idempotent by manifest hash, with a separate QMD_INSTALL_TIMEOUT for the one-time native build and observable install failures. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/qmd_index.sh:169-250 (_qmd_ensure_prefix, _qmd_run) |
| Alpine musl native toolchain + bigstack.so | 016 | partial — only qmd-upgrade-checklist.md references bigstack/the build recipe (as checklist items); architecture.md's image/privilege sections say nothing about the toolchain or the build-arg | architecture | The image bakes build-base/cmake/linux-headers/libgomp gated by build-arg QMD_NATIVE_TOOLCHAIN=1 so node-llama-cpp compiles from source on musl (GGML_NATIVE=OFF, armv8-a), plus an LD_PRELOAD pthread shim (bigstack.so, 8MB stacks) applied only to qmd embed against the musl std::regex stack hazard. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/docker/Dockerfile:56-68 + docker/bigstack.c |
| qmd MCP server from the managed prefix (qmd_mcp_exec + per-mode wrappers) | 016 | no — worse than absent: vault.md:311-313 documents the retired shape ("args": ["@tobilu/qmd@latest", "mcp"]), and adding-an-mcp.md is silent | vault | The qmd MCP server Claude searches with launches via qmd_mcp_exec (no timeout, bigstack, same prefix + storage as the reindex writer) behind image-baked docker/scripts/qmd-mcp or rendered scripts/local/agent-qmd-mcp.sh; .mcp.json's qmd entry is command={{QMD_MCP_COMMAND}} with args=[] — no longer args=[@tobilu/qmd@X, mcp]. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/qmd_index.sh:258 (qmd_mcp_exec) + docker/scripts/qmd-mcp + modules/local-qmd-mcp.sh.tpl + modules/mcp-json.tpl:74 |
| qmd version pin guardrail + upgrade checklist | 016 | partial — docs/qmd-upgrade-checklist.md exists and is accurate for 016, but has no sqlite-vec pair item (017) and no embed-loop/pending re-verification item (018) | qmd-upgrade-checklist | Bumping vault.qmd.version is a deliberate, checklisted change: tests fix the known-good version string (and 017 adds the qmd 2.5.3 <-> sqlite-vec 0.1.9 pair) so a silent bump — e.g. to 2.6.x where tree-sitter became hard deps — fails the suite before it breaks musl builds. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/tests/qmd-version-guard.bats + tests/qmd-sqlite-vec.bats |
| musl sqlite-vec compile + runtime swap | 017 | no — 'sqlite-vec'/'vec0' appear in no doc, including the upgrade checklist that guards the paired pin | qmd-upgrade-checklist | The image compiles the pinned sqlite-vec v0.1.9 amalgamation for musl (sha256-verified, -Du_int8_t=uint8_t shim) into /opt/agent-admin/sqlite-vec/vec0.so, and _qmd_ensure_prefix swaps out the glibc prebuilt in the prefix — musl-gated, cmp-idempotent, no-op on glibc, fail-silent if the artifact is absent — making semantic embed/vsearch work in docker. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/docker/scripts/build-sqlite-vec.sh + scripts/lib/qmd_index.sh:128 (_qmd_swap_sqlite_vec) |
| Multi-pass embed completion + resumable guard + extended qmd state | 018 | no — 'partial'/'stalled' statuses, 'pending', and the multi-pass loop appear in no doc (state-layout.md also lacks any qmd-index.json schema entry) | vault | Large-vault embeds survive qmd's hardcoded 30-min session cap: _qmd_embed_until_complete re-invokes qmd embed in fresh sessions (up to QMD_EMBED_MAX_PASSES=12) until pending=0, stall, or cap; an unchanged vault now skips embedding ONLY when pending==0 (otherwise it resumes); qmd-index.json gains a pending count and last_status values partial/stalled alongside indexed/skipped/error. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/scripts/lib/qmd_index.sh:463-530 (_qmd_embed_until_complete, _qmd_pending_count; state at :287-307) |
| Canonical qmd test seam (contributor-facing, test-only) | 019 | no — the seam contract lives only in specs/019-fix-qmd-test-drift/contracts/qmd-test-seam.md; no docs/ file covers testing conventions | architecture | Contributors stub the qmd engine via tests/helper.bash::install_qmd_stub{,_fail}, which plant a fake qmd inside the managed prefix with a pre-seeded .installed-hash and emit the 018 completion signal — exercising the real _qmd_run/_qmd_ensure_prefix skip path instead of a dead bunx stub. No runtime behavior. | /Users/rodrigo-hinojosa/Documents/Cencosud/Claude/Agents/agentic-pod-launcher/tests/helper.bash:55-90 |
