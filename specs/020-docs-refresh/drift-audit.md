# Drift Audit: docs vs v0.12.0 (`33bfb74`)

Phase 0 artifact (FR-005) — generated from workflow `wf_a96ac163-11f` (16 agents,
475 verification tool calls, 2026-07-12). This is the SC-001 oracle: after the
refresh, every finding below must be re-checked against the updated doc and no
falsehood may survive. Verdicts: **false** (contradicts current code), **stale**
(was true, no longer), **needs-qualifier** (true in one mode/context only),
**unverified-suspicion** (auditor could not verify — resolve during implement).

**Totals**: 121 findings across 14 docs; coverage gaps per doc listed after each table.


## README.md

_README.md is an accurate but frozen pre-011 snapshot: nearly all docker-mode mechanics it describes still verify against the code (agentctl/heartbeatctl subcommands, backup schedules 30 3 * * * / hourly, heartbeat 30m default + 3500-char cap + tmux isolation, crontab sync ~75s, yq vendoring, @bitbonsai/mcpvault, purge semantics). Its two real problems are (1) the complete absence of the local/systemd deployment mode and the entire QMD/RAG + wiki-graph subsystem shipped in 011-018, which makes the front-door mental model wrong, and (2) a handful of stale specifics — the broken RESTORE_IDENTITY_KEY env-var advice (only --identity-key works), the 120s typing cap (now v4: 5-min env-tunable cap with user-facing warning), ~220 tests (now 977), alpine 3.20 (now 3.24.1), and '12 green checks' (now ~20)._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | stale | Tagline/'What this is' present the launcher as Docker-only: "scaffolds persistent Claude Code agents in Docker"; wizard "prompts for agent personality, plugins, MCPs, notification channel, and heartbeat schedule"; "That workspace contains a docker-compose.yml" _(Lines 3-9 (tagline + 'What this is'))_ | setup.sh:443-452 — wizard section '── 0. Deployment mode (011)' runs FIRST: `deploy_mode=$(ask_choice "Deployment mode" "docker" "docker local")`; setup.sh:449 'local — runs directly on this host via systemd (Linux/systemd only)'; scripts/agentctl:1228-1240 has a full local-mode dispatch (cmd_local_status/cmd_local_doctor/cmd_local_heartbeat); modules/ contains 20+ local-*.tpl templates and systemd-remote-control.service.tpl. In local mode no docker-compose.yml is scaffolded (setup.sh:1807 skips the 'docker' item when mode=local). | State up front that the wizard's first prompt selects a deployment mode (docker \| local/systemd, since feature 011) and qualify the docker-compose/workspace description as docker-mode. |
| 2 | medium | needs-qualifier | Prerequisites: "Docker 24+ with the Compose v2 plugin" listed unconditionally as a requirement _(Prerequisites, line 13)_ | setup.sh:443-459 — deployment mode 'local' exists and is 'Linux/systemd only'; local mode runs the agent directly on the host (systemd-remote-control.service.tpl, modules/local-bootstrap.sh.tpl) with no Docker involvement; agentctl in local mode refuses docker-only subcommands (agentctl:1233 'up\|start\|stop\|restart\|ps\|attach\|shell\|run\|logs\|mcp) _local_docker_only'). | Qualify Docker as a docker-mode prerequisite and add the local-mode row (Linux with systemd; no Docker needed). |
| 3 | medium | false | "pass `RESTORE_IDENTITY_KEY=/path/to/private-key` env var to `setup.sh`" as the recovery when no SSH key exists on the new host _(Restore walkthrough > Common gotchas, line 192)_ | setup.sh:356 unconditionally initializes `RESTORE_IDENTITY_KEY=""` before argument parsing, clobbering any environment value; the variable is only ever set by the `--identity-key PATH` flag (setup.sh:419 `--identity-key) RESTORE_IDENTITY_KEY="$2"`). Passing it as an env var silently does nothing and decryption falls back to the standard key pair (setup.sh:1649-1654). | Replace the env-var advice with the actual flag: `./setup.sh --restore-from-fork <url> --identity-key /path/to/private-key`. |
| 4 | medium | stale | Typing indicator: "adds a 4-second refresh interval (with a 120-second hard cap)" _(Two-way Telegram chat, first bullet, line 71)_ | docker/scripts/apply_telegram_typing_patch.py — patch is at v4 (line 61 MARKER 'typing refresh patch v4'). The 120s cap was v1-only and is actively REMOVED by the v1→v2 upgrader (lines 313-318 delete `const _TYPING_MAX_MS = 120000`). v4 (lines 112-150) has `_TYPING_MAX_DURATION_MS` defaulting to 300000 ms (5 min), overridable via env `TELEGRAM_TYPING_MAX_MS`, and on expiry aborts the interval, sends a user-facing warning message to the chat, and logs to stderr. | Describe the v4 contract: 4s refresh until reply fires; aborts after 5 minutes by default (TELEGRAM_TYPING_MAX_MS) with a user-visible timeout warning instead of a silent 120s stop. |
| 5 | medium | needs-qualifier | Derived files are "docker-compose.yml, .mcp.json, CLAUDE.md, scripts/heartbeat/heartbeat.conf, .env skeleton, NEXT_STEPS.md" (presented as the complete set) _(Scaffolding from agent.yml, line 55)_ | setup.sh:2219-2257 — in local mode, regenerate also renders `.state/remote-control.env` plus `scripts/local/agent-{login,killswitch,healthcheck,bootstrap,qmd-reindex,qmd-watch,qmd-mcp,vault-backup,wiki-graph}.sh`, and install_service emits the systemd unit(s) (setup.sh:2351-2353). None of these appear in the README list. | Add a clause that local mode additionally derives scripts/local/ wrappers, .state/remote-control.env and the systemd units from agent.yml. |
| 6 | low | stale | "bats tests/ # full suite (~220 tests)" and coverage list (render engine, YAML lib, interval converter, state-lib, notifiers, heartbeat runner, plugin catalog, Telegram patcher, config-dir isolation, heartbeatctl) _(Testing section, lines 247-252)_ | grep -c '@test' tests/*.bats sums to 977 across ~95 files; tests/ now includes deployment-mode.bats, local-*.bats (bootstrap, render, schedule, qmd, vault-backup, wiki-graph, killswitch...), qmd-*.bats, wiki-graph.bats, backup-*.bats, token-health-*.bats, docker-e2e-qmd.bats — none reflected in the coverage description. | Update the count to ~977 and extend the coverage sentence with local mode, QMD/RAG, wiki-graph, backups, and token health. |
| 7 | low | stale | "./scripts/agentctl doctor   # 12 green checks if everything is right" _(Restore walkthrough, step 5, line 188)_ | scripts/agentctl cmd_doctor (lines 334-576) now runs checks numbered 1-12 PLUS 5b (launcher version stamp), 12b (state bind-mount), 13-15 (backup freshness for identity/vault/config), 15b (plugin install failures), and 16-18 (token health) — a healthy agent prints roughly 20 checks, not 12. | Drop the hardcoded count (e.g. 'all checks green'). |
| 8 | low | stale | Architecture diagram: "CONTAINER (alpine 3.20, agentic-pod:latest)" _(Architecture summary diagram, line 214)_ | docker/Dockerfile:5 `ARG BASE_IMAGE=alpine:3.24.1`. (The image tag agentic-pod:latest is still correct — setup.sh:1057.) | Update to alpine 3.24 or remove the pinned version from the diagram. |
| 9 | low | needs-qualifier | Auto-memory lives at `<workspace>/.state/.claude/projects/-workspace/memory/` _(Memory persistence, first bullet, line 129)_ | The `-workspace` project-dir name derives from the container cwd `/workspace` (docker mode). In local mode the agent runs with `WorkingDirectory={{DEPLOYMENT_WORKSPACE}}` (modules/systemd-remote-control.service.tpl:11), so the sanitized project dir is the host workspace path, not `-workspace`. | Mark the `-workspace` path segment as docker-mode; note local mode uses the sanitized host workspace path. |
| 10 | low | needs-qualifier | doctor "checks Docker daemon, container status, healthcheck, agent.yml, .env, tmux, crond, the Telegram plugin, the heartbeat, the vault, and the plugin patches" _(Quickstart, line 43)_ | scripts/agentctl:554-576 — doctor also checks backup freshness (identity/vault/config), plugin install failures, token health, state bind-mount, and launcher version stamp; in local mode a separate cmd_local_doctor runs against systemd with the 013 exit contract 0/1/2 (agentctl:1172-1187, comment '013 FR-009: honest exit codes'). | Extend the list with backup freshness and token-health checks, and note doctor has a systemd-backed local-mode variant with exit codes 0/1/2. |

**Coverage gaps**:
- Local/systemd deployment mode (011/012): no mention anywhere that the wizard's first prompt selects docker|local, what local mode is (systemd Remote Control unit, agent-bootstrap.sh, scripts/local/ wrappers, setup.sh --login), or how agentctl behaves in local mode (docker-only subcommands refuse; status/doctor read systemd; heartbeat supports only qmd-reindex/backup-vault/wiki-graph locally)
- QMD semantic RAG over the vault (010/012/013/016-018): the 'Memory persistence' vault bullet and 'What's in the box' never mention the qmd MCP server (.mcp.json qmd entry via QMD_MCP_COMMAND wrappers), the reindex/watch schedules (cron in docker, systemd timers in local), or that qmd storage persists under .state/.cache
- Wiki-graph layer (014): derived <vault>/.graph/{graph,backlinks,findings}.json, `heartbeatctl wiki-graph` / `agentctl heartbeat wiki-graph`, and the default 20 */6 schedule are absent
- heartbeatctl runtime-CLI section omits the subcommands added since 013/014 that the code ships: backup-identity, qmd-reindex, wiki-graph (dispatch at docker/scripts/heartbeatctl:1053-1057) — backup-identity is only mentioned in passing in the backup section
- Uninstall section is docker-framed; setup.sh --uninstall in local mode also removes 'installed services, agent scripts, timers, tmux' (setup.sh usage line 374) — no mention of systemd unit/timer removal

## docs/agentic-quickstart.es.md

_El doc está congelado pre-011: el detalle campo a campo sigue siendo correcto (catálogos de 6 MCPs y 5 plugins, validadores, defaults, vendoring de yq/gh, URL del template), pero le falta el prompt de deployment mode que desde 011 es la PRIMERA pregunta del wizard — seguir la secuencia de stdin documentada desincroniza en la línea 1 y cuelga el wizard, rompiendo la promesa central del doc de ser espejo de wizard_answers(). El test de drift (quickstart-doc.bats) no tiene token para ese prompt, por eso nunca falló; hallazgos secundarios: ejemplo de intervalo "1d" inválido, claim exagerado de "rechaza vacío", guía post-scaffold docker-only y sección Telegram host-céntrica sin calificar._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | stale | El orden del stdin es "espejo de tests/helper.bash::wizard_answers()" y arranca en "1. Identity (4 líneas): AGENT_NAME..." — sin prompt de deployment mode _(Bloque 2 — CONSTRUCCIÓN DEL STDIN (líneas 134-163); Bloque 1 sección "Deployment" (líneas 57-59) tampoco tiene variable DEPLOYMENT_MODE)_ | setup.sh:451 `deploy_mode=$(ask_choice "Deployment mode" "docker" "docker local")` se pregunta como paso 0, ANTES de "▸ Agent identity" (setup.sh:464); tests/helper.bash:131-132 emite `printf '%s\n' "$deployment_mode"` como PRIMERA respuesta con el comentario "Deployment mode choice (011) — asked FIRST, on all platforms". Pipear la secuencia documentada entrega AGENT_NAME a un ask_choice que solo acepta docker\|local → re-prompt en loop → todo el stdin se desincroniza y el wizard queda colgado. El test de drift tests/quickstart-doc.bats:22-38 no tiene token para este prompt, por eso no falló. | Agregar DEPLOYMENT_MODE="docker" al Bloque 1 y un paso "0. Deployment mode (1 línea): docker\|local" como primera línea del stdin en el Bloque 2 (y sincronizar el doc EN + la keyword list de quickstart-doc.bats). |
| 2 | medium | false | HEARTBEAT_INTERVAL="30m" # 5m, 30m, 1h, 6h, 1d, etc. _(Bloque 1, línea 87)_ | scripts/lib/wizard-validators.sh:62 solo acepta `^[0-9]+[mh]$` o cron de 5 campos (líneas 60-70), y setup.sh:802 pasa el prompt por `ask_validated ... validate_cron_or_interval` — `1d` se rechaza y re-promptea (desincroniza el stdin pipeado). La propia tabla de validaciones del doc (línea 203) dice `Nm`/`Nh` o cron, contradiciendo este comentario. | Reemplazar "1d" por un ejemplo válido como "0 9 * * *" (cron) o eliminarlo. |
| 3 | medium | needs-qualifier | EJECUCIÓN y resumen post-scaffold asumen solo docker: "docker-compose render" como paso del scaffold, pendientes "push inicial, /login, pairing de Telegram" _(Bloque 2 — EJECUCIÓN (líneas 165-175))_ | setup.sh:1390-1398 bifurca el render de NEXT_STEPS por `deployment.mode` (docker vs local); setup.sh:2773 "ERROR: --login is only for local mode" — en modo local el login es `./setup.sh --login` (setup.sh:564-566) y no hay render de docker-compose ni /login en tmux. | Al agregar el paso 0 de deployment mode, calificar los pasos post-scaffold por modo (docker: compose build/up + /login en tmux; local: systemd + ./setup.sh --login). |
| 4 | low | needs-qualifier | Tabla de campos: "El wizard rechaza vacío en estos" para AGENT_NAME, USER_NAME, EMAIL, DESTINATION _(Tabla de campos, fila Required (línea 187))_ | setup.sh:474 `ask "Agent name (lowercase, no spaces)" "my-agent"` y setup.sh:540 `ask "Agent destination directory" "$default_dest"` — una línea vacía toma el default silenciosamente en vez de ser rechazada; solo USER_NAME (ask_required, setup.sh:503) y EMAIL (validate_email rechaza vacío) fallan de verdad. | Aclarar que AGENT_NAME y DESTINATION vacíos toman default (my-agent / ruta derivada) — el agente que pipea debe imponer no-vacío por su cuenta, no confiar en el wizard. |
| 5 | low | needs-qualifier | Sección Telegram: requiere instalar bun con curl, habilitar enabledPlugins en ~/.claude/settings.json y reiniciar Claude Code completo _(Sección "Telegram (chat bidireccional)" (líneas 222-251))_ | modules/plugins/telegram.yml:3 `type: default` — el plugin viene en el catálogo default del agente scaffoldeado (auto-instala post-/login); docker/Dockerfile:127-140 hornea bun en la imagen ("Install bun (required by the official telegram plugin...)"); docker/scripts/start_services.sh:407 muestra que el flujo del pod maneja `/telegram:configure` (el wizard in-container del watchdog). Los pasos descritos aplican a una sesión Claude Code del host, no al pod docker. | Prologar la sección aclarando que en el pod docker bun y el plugin ya vienen resueltos (el pairing se guía desde NEXT_STEPS/wizard in-container) y que estos pasos manuales aplican a una sesión host o modo local. |
| 6 | low | stale | El slash /quickstart "te pide los valores mínimos ... (AGENT_NAME, USER_NAME, EMAIL, DESTINATION, opcionalmente FORK_* y VAULT_*)" _(Sección "Atajo: el slash command /quickstart" (línea 13))_ | .claude/commands/quickstart.md:21-27 marca como obligatorios AGENT_NAME, DISPLAY_NAME, ROLE, USER_NAME y EMAIL (DESTINATION es opcional con default), y además pregunta por MCPs y plugins opcionales. | Actualizar la lista de mínimos a AGENT_NAME, DISPLAY_NAME, ROLE, USER_NAME, EMAIL (DESTINATION opcional con default). |

**Coverage gaps**:
- 011 — Prompt de deployment mode (docker|local): falta la variable DEPLOYMENT_MODE en el Bloque 1, la primera línea del stdin en el Bloque 2, y la advertencia de que local es Linux/systemd only con el warning de seguridad (el agente corre como tu usuario, sin aislamiento).
- 011/012 — Flujo local en pre-flight y post-scaffold: en modo local Docker no es prerequisito, el login es `./setup.sh --login` (no /login en tmux), y el resultado son unidades/timers systemd en vez de `docker compose build && agentctl up`; NEXT_STEPS.md se renderiza por modo.
- 011 — Fila en la tabla "Validaciones aplicadas por el wizard" para el nuevo prompt: ask_choice solo acepta `docker`/`local`; cualquier otro valor re-promptea y desincroniza el stdin pipeado (exactamente el modo de falla que la tabla ya advierte para otros campos).

## docs/agentic-quickstart.en.md

_The doc is accurate in almost all of its verifiable detail (validators, MCP/plugin catalogs, vendoring of yq/gh, vault prompts, flags), but it froze before feature 011 and misses the deployment-mode prompt that is now the FIRST wizard question on all platforms — following its stdin recipe verbatim desyncs the piped wizard from line one, which defeats the doc's core purpose. Everything else 012-019 touched is invisible at the wizard-input level, so the remaining findings are per-mode qualifiers (local vs docker post-scaffold steps) and small precision issues. ES/EN parity is intact — both versions share the identical 011 drift — except the EN 'loads this doc' sentence, since /quickstart only loads the ES version._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | STDIN BUILD — respect this EXACT order (mirror of tests/helper.bash::wizard_answers()) — order starts at '1. Identity (4 lines): AGENT_NAME, DISPLAY_NAME, ROLE, VIBE' _(Block 2 — Instructions, STDIN BUILD list (lines 133-162))_ | setup.sh:443-451 asks 'Deployment mode' FIRST via ask_choice "Deployment mode" "docker" "docker local" (comment: 'Asked FIRST'); tests/helper.bash:131-132 emits printf '%s\n' "$deployment_mode" before Identity. ask_choice (scripts/lib/wizard.sh:56-68) loops consuming stdin lines until one matches 'docker' or 'local' — piping the doc's stdin makes the first prompt eat AGENT_NAME, DISPLAY_NAME, etc., desyncing the entire wizard. | Add a step 0 line to the stdin order: DEPLOYMENT_MODE (docker\|local, 1 line, asked first on all platforms — feature 011). |
| 2 | high | stale | Block 1 configuration covers every wizard input (Deployment section only has DESTINATION and INSTALL_SERVICE); field-reference table lists all safe defaults _(Block 1 — Configuration (lines 43-105) and Field reference table (lines 182-189))_ | setup.sh:443-461: the wizard's first prompt is deployment mode (docker\|local, default docker, with a local-mode security warning); schema.sh:54 declares '.deployment.mode=docker,local'. No DEPLOYMENT_MODE variable exists in Block 1 or the defaults table. | Add DEPLOYMENT_MODE="docker" (docker \| local; local = systemd on Linux only) to Block 1's Deployment section and to the safe-default row of the field-reference table. |
| 3 | medium | needs-qualifier | On success: summarize live branch created, fork URL, pending commands (initial push, /login, Telegram pairing, MCP validation) _(Block 2 — EXECUTION step 15 (lines 171-174))_ | setup.sh:1390-1393: 'NEXT_STEPS branches docker vs local' on deployment.mode; setup.sh:563-566: in local mode login is './setup.sh --login' on the host (not /login inside tmux); setup.sh:2771-2774: --login is only for deployment.mode=local. The summary as written only describes the docker flow. | Qualify the pending-commands summary per deployment mode: docker = docker compose build/up + /login inside tmux; local = ./setup.sh --login + systemd units. |
| 4 | low | needs-qualifier | The /quickstart command loads this doc + tests/helper.bash::wizard_answers() as reference _(Section 'Shortcut: the /quickstart slash command' (line 13))_ | .claude/commands/quickstart.md:11 loads only 'docs/agentic-quickstart.es.md' — the EN doc is never loaded by the command, so 'this doc' is inaccurate in the EN version (the identical ES sentence is accurate). This is the only real ES/EN parity break. | In the EN doc, say the command loads the Spanish counterpart (docs/agentic-quickstart.es.md) plus wizard_answers(). |
| 5 | low | needs-qualifier | Required (no safe default): AGENT_NAME, USER_NAME, EMAIL, DESTINATION — 'Wizard rejects empty input here.' _(Field reference table, Required row (line 186))_ | setup.sh:474: agent name prompt has default 'my-agent' (empty input is accepted as the default, not rejected); setup.sh:540: destination prompt has a computed default path. Only USER_NAME (ask_required, setup.sh:503) and EMAIL (ask_validated with no default, setup.sh:513) actually reject empty input. | Reword to: USER_NAME and EMAIL reject empty input; AGENT_NAME and DESTINATION fall back to generic defaults you almost never want, so treat them as required. |
| 6 | medium | stale | 'mirror of tests/helper.bash::wizard_answers(), which is the canonical source of truth, kept in sync by every PR' _(Block 2 — STDIN BUILD preamble (lines 133-135))_ | The doc itself is out of sync with wizard_answers() (tests/helper.bash:131-132 added the deployment_mode line in feature 011; this doc was never updated), so the 'kept in sync by every PR' guarantee is demonstrably not holding for the docs. | Keep the sentence but fix the actual desync (add the deployment-mode line), and consider pointing readers to wizard_answers() as the tiebreaker when the doc disagrees. |

**Coverage gaps**:
- Deployment mode (011): no mention anywhere that the wizard's first prompt is docker|local, that local mode is Linux/systemd only, or that choosing local prints a security warning (agent runs as your login user, MFA mandatory) — this doc's whole role is enumerating wizard inputs
- Local-mode post-scaffold flow (011/012): './setup.sh --login' one-time host login, systemd units instead of 'docker compose build && up', and mode-branched NEXT_STEPS.md — the Steps and success-summary sections describe only the docker flow
- Field reference and validation tables lack the deployment-mode value rule (must be literally 'docker' or 'local'; any other value makes ask_choice re-prompt and desync piped stdin — the exact failure mode the validation section warns about for other fields)

## docs/getting-started.md

_The core docker flow (first boot, /login, Telegram wizard, pairing, teardown semantics, privilege gotchas) is still accurate, and the 011 local-mode section is present and mostly correct (MIN_VERSION 2.1.51, killswitch path, healthcheck ~5min, quickstart link all verified). The doc's biggest drift is the Upgrade/Rollback section, which predates the workspace-owned docker/ build context and fork sync: it names a nonexistent repo dir and image tag and describes an update path that is a no-op today. Secondary drift: the scaffold section misstates which secrets the host wizard collects and buries the (now-first) deployment-mode prompt, the Rotating Secrets flow overstates what 'docker compose restart' applies, and nothing from 012-014 (vault/QMD prompts, agentctl doctor, backup/restore-from-fork) has reached this doc._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | Upgrade flow: 'cd agent-admin-template && git pull' then 'docker compose build' in the workspace picks up template updates _(## Upgrade, lines 149-163)_ | modules/docker-compose.yml.tpl:9 sets build 'context: ./docker' (the WORKSPACE's own copy); setup.sh:1806-1828 copies the docker/ tree into the destination at scaffold time, so 'git pull' in the launcher clone never reaches an existing workspace. The supported update path is './setup.sh --sync-template' (setup.sh:369-371, 410). Also the repo is named agentic-pod-launcher, not agent-admin-template. | Replace the section with the real flow: './setup.sh --sync-template' (fork-based agents) or re-run scaffold/--regenerate, then 'docker compose build && docker compose up -d' in the workspace. |
| 2 | medium | false | Tag/rollback the image as 'agent-admin:latest' / 'agent-admin:prev' _(## Upgrade line 153 and ## Rollback lines 169-176)_ | setup.sh:1057 records the default image tag as 'agentic-pod:latest' in agent.yml's docker block; modules/docker-compose.yml.tpl:6 uses 'image: {{DOCKER_IMAGE_TAG}}'. No 'agent-admin' image name exists in the launcher. | Use the tag from agent.yml docker.image_tag (default agentic-pod:latest) in both the backup-tag and rollback commands. |
| 3 | medium | stale | "Sensitive tokens (Telegram bot token, chat ID, GitHub PAT) are not requested here. They are deferred to the container's first-run wizard." _(## Scaffold, line 30)_ | The host wizard now optionally captures: heartbeat bot token (setup.sh:632) and chat ID with auto-discovery (setup.sh:645-668), Atlassian API tokens (setup.sh:761), GitHub PAT for MCP (setup.sh:791), fork PAT (setup.sh:594), and per-MCP secrets (setup.sh:733). docker/scripts/wizard-container.sh:117-128 explicitly skips GITHUB_PAT 'already set by host wizard'. Only the chat-plugin Telegram bot token is truly deferred. | Qualify: only the chat-plugin Telegram bot token defers to the container wizard; the host wizard optionally captures GitHub PAT, Atlassian tokens, and heartbeat notifier token/chat ID (Enter to skip, fill .env later). |
| 4 | medium | stale | Wizard step list is 'name, personality, MCPs, notification channel', with deployment mode mentioned as an afterthought ('The wizard also asks for a deployment mode') after the Output section _(## Scaffold, lines 23-28 and 38)_ | setup.sh:443-451 — deployment mode is prompt 0, commented 'Asked FIRST so the two flows stay cleanly separated'. Actual sequence: mode → identity → about-you → destination/systemd → Claude profile → GitHub fork → heartbeat notifications → MCPs → heartbeat feature → principles → knowledge vault/QMD (setup.sh:819-836) → optional plugins. Notification channel precedes MCPs, not the reverse. | List deployment mode as the first prompt and update the sequence (at minimum add the vault/QMD and optional-plugins prompts). |
| 5 | medium | needs-qualifier | Rotating secrets: edit .env then 'docker compose restart (applies new env vars)'; 'Changes take effect immediately' _(## Rotating Secrets, lines 180-190)_ | .env is injected via env_file at container creation (modules/docker-compose.yml.tpl:67-68); .mcp.json consumes GITHUB_PAT/ATLASSIAN_* via ${VAR} expansion from process env (modules/mcp-json.tpl:53-65); grep of docker/scripts/start_services.sh and docker/entrypoint.sh shows no re-sourcing of /workspace/.env — only TELEGRAM_BOT_TOKEN is re-synced per respawn (start_services.sh:691 ensure_channel_env_synced). 'docker compose restart' does not recreate the container, so env_file changes are not re-applied. | State that restart suffices only for TELEGRAM_BOT_TOKEN (supervisor re-syncs it); GitHub PAT / Atlassian tokens need 'docker compose up -d --force-recreate'. |
| 6 | medium | stale | Telegram plugin failed → 'Run the one-time setup from "One-time Claude authentication and plugin setup" above, then docker compose restart' _(## Troubleshooting, plugin:telegram failed, line 259)_ | docker/scripts/start_services.sh:655-663 — next_tmux_cmd calls ensure_all_plugins_installed on every respawn, auto-installing agent.yml plugins after /login; modules/next-steps.en.tpl:232 documents 'the watchdog detects it and auto-installs the plugins + re-launches with --channels — no manual restart needed'. The referenced heading 'One-time Claude authentication and plugin setup' does not exist anywhere in this doc. | Say the watchdog auto-installs plugins after /login (check .state/plugin-install-failures.jsonl or 'agentctl doctor' if one stays failed) and remove the dead cross-reference. |
| 7 | medium | needs-qualifier | Local mode: 'journalctl -u agent-<name>.service -f  # look for session url / connected' _(## Local standalone mode, line 61)_ | modules/local-healthcheck.sh.tpl:40-43: 'A healthy --spawn=session is SILENT in the journal, so grepping it for session url\|connected\|polling false-WARNed on every tick even when connected (validated on mclaren). The live socket owned by the unit's MainPID is the real signal.' | Note that a healthy connected session logs nothing after startup — verify via the healthcheck / 'agentctl status' (live ESTABLISHED :443 socket), not by waiting for 'connected' in the journal. |
| 8 | medium | needs-qualifier | Prerequisites: Docker v24+, Compose v2, ~2GB disk, Bash 4+ (only) _(## Prerequisites, lines 7-12)_ | setup.sh needs host yq v4+ (auto-vendored by yaml_require_yq, scripts/lib/yaml.sh:112-116), jq (used at setup.sh:664 and by agentctl), git, and optionally gh for the fork prompt (setup.sh:584). Local mode needs no Docker at all (setup.sh:2101 deletes docker-compose.yml in local mode) but requires Linux/systemd and Claude Code >= 2.1.51 on the host (modules/local-login.sh.tpl:13 MIN_VERSION). | Split prerequisites per mode: docker mode (Docker/Compose + yq/jq/git on host), local mode (Linux/systemd, Claude Code >= 2.1.51, no Docker). |
| 9 | low | needs-qualifier | Output: '~/agents/<name>/' as the workspace location _(## Scaffold, Output, line 34)_ | setup.sh:539-541 — default destination is '<parent-of-installer-clone>/agents/<agent_name>' (installer_parent=$(dirname "$SCRIPT_DIR")), not the user's home. All later '~/agents/<name>' commands inherit the same assumption. | Say the destination defaults to a sibling 'agents/' directory next to the launcher clone (overridable with --destination). |
| 10 | low | needs-qualifier | Output: '/etc/systemd/system/agent-<name>.service — host unit to manage the container' (listed unconditionally) _(## Scaffold, Output, line 35)_ | setup.sh:552-557 — on non-Linux the unit is skipped ('Host systemd unit: skipped on Darwin'); on Linux it's behind the opt-in prompt 'Install as system service?'. | Qualify as Linux-only and opt-in (skipped on macOS, where restart-on-login relies on Docker Desktop + unless-stopped). |
| 11 | low | needs-qualifier | Teardown 'Removes the host systemd unit (Linux only)' / --nuke leaves 'no systemd units' _(## Teardown, lines 204 and 219)_ | setup.sh:2618-2628 — units are removed only when passwordless sudo is available (sudo -n true); otherwise uninstall prints 'local systemd units present — remove manually with sudo (agent-<name>*.{service,timer})' and leaves them. | Add that unit removal requires sudo; without it uninstall prints manual removal instructions. |
| 12 | low | needs-qualifier | '/workspace/claude.cron.log  # crond heartbeat log' _(## Troubleshooting, Container logs, line 328)_ | docker/entrypoint.sh:107 — claude.cron.log is busybox crond's own daemon log ('crond -b -L /workspace/claude.cron.log'); heartbeat tick output goes to /workspace/scripts/heartbeat/logs/cron.log (docker/crontab.tpl:5). | Label it as the crond daemon log and add scripts/heartbeat/logs/cron.log for heartbeat output. |
| 13 | low | false | UID check: 'grep "docker:" ~/agents/<name>/agent.yml  # should contain matching UID' _(## Troubleshooting, UID mismatch, line 310)_ | The uid lives under the docker: block as docker.uid (setup.sh:1057-1059 writes 'uid: $(id -u)' nested under docker:); grepping the literal 'docker:' block-header line prints no UID at all, so the command cannot verify what the doc claims. | Use "yq '.docker.uid' agent.yml" (or grep 'uid:') instead. |
| 14 | low | needs-qualifier | Intro: 'Each agent runs inside its own Docker container with all state stored under the workspace directory' _(Intro, line 3)_ | Since 011 the wizard's first prompt selects deployment.mode docker\|local (setup.sh:443-451); in local mode there is no container — the agent runs on the host under systemd (setup.sh:2351-2359). The doc covers local mode later but the framing sentence asserts docker universally. | Qualify the opening sentence with the two deployment modes (docker container vs local systemd session). |

**Coverage gaps**:
- Knowledge vault + QMD hybrid search wizard prompts (010/012/013): the scaffold section never mentions the vault opt-in or the QMD prompt (BM25+vector+rerank, ~300MB embedding model on first use), which every user hits during the wizard (setup.sh:819-836)
- Backup/restore (identity/vault/config orphan branches + './setup.sh --restore-from-fork <url>'): the Teardown/Upgrade/migration story never mentions that a re-install or host migration can be restored from the agent's fork
- agentctl as the day-2 interface (013 ops parity): the doc drives everything via raw 'docker exec … tmux attach' and never mentions './scripts/agentctl up|attach|logs|status' in docker mode, nor 'agentctl doctor' (exit 0/1/2 contract) as the post-boot verification step in both modes
- Local-mode companion units (012/014): when vault/QMD are enabled, --login/install also enables qmd-reindex/qmd-watch/vault-backup/wiki-graph timers (setup.sh:2432-2480); the local section mentions only the healthcheck timer, and the kill-switch actually stops session + qmd + backup + healthcheck (modules/next-steps.en.tpl:304), not just the session
- NEXT_STEPS.md as scaffold output: the rendered per-agent instruction file (en/es) is not listed in the Output section even though it is the primary post-scaffold guide

## docs/architecture.md

_El doc está sólido y al día hasta la feature 014 (los párrafos 011-014 de modos de despliegue, RAG local y wiki-graph verificaron bien contra el código), pero quedó congelado antes de 015-018: la descripción del stack QMD todavía dice "invoked via bunx" (invertido por 016), el guard de embed ignora el loop reanudable de 018, y no existe cobertura del prefijo gestionado, sqlite-vec musl (017) ni TMPDIR bajo .state (015). Además arrastra artefactos pre-renombre que rompen flujos operativos: el flujo de Upgrade apunta a un "agent-admin-template" inexistente y a la imagen "agent-admin:latest" (el default real es agentic-pod:latest y el workspace es autocontenido, así que ese upgrade reconstruye silenciosamente código viejo). El modelo de privilegios, watchdog (5 crashes/300s, poll 2s), tmpfs 100MB, heartbeat (retención 20 sesiones, rotación 10MB) y las unidades systemd locales verificaron correctos._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | QMD registrado como segundo MCP server "(@tobilu/qmd, pinned via vault.qmd.version, invoked via bunx)" _(Sección "Vault layer", párrafo QMD (línea 279))_ | scripts/lib/qmd_index.sh:92-96 ("016: qmd is installed from a MANAGED bun prefix, not `bunx`"); modules/mcp-json.tpl:73-76 (entrada qmd: command={{QMD_MCP_COMMAND}}, args=[], env={{QMD_MCP_ENV}}); setup.sh:2018-2022 (QMD_MCP_COMMAND=/opt/agent-admin/scripts/qmd-mcp en docker, scripts/local/agent-qmd-mcp.sh en local); docker/scripts/qmd-mcp (wrapper que ejecuta qmd_mcp_exec desde el prefijo gestionado, explícitamente NO bunx) | Reemplazar "invoked via bunx" por: QMD corre desde un prefijo bun-install gestionado bajo el cache root (016), y el MCP server se lanza vía el wrapper qmd-mcp (docker) / agent-qmd-mcp.sh (local) referenciado en .mcp.json. |
| 2 | high | false | Flujo de upgrade: "cd agent-admin-template && git pull" seguido de "cd ~/agents/<name> && docker compose build" actualiza el agente _(Sección "Upgrade & Rollback" (líneas 158-171))_ | setup.sh:1802-1809 copia setup.sh, VERSION, modules/, scripts/ y docker/ DENTRO del workspace al scaffold (workspace autocontenido); modules/docker-compose.yml.tpl:9 (build context: ./docker, la copia del workspace). Un git pull en el clon del launcher no toca el workspace existente: docker compose build reconstruiría silenciosamente con el contexto viejo. Además no existe ningún "agent-admin-template" en el repo (grep -rn agent-admin-template → 0 hits fuera del doc). | Documentar que el workspace es autocontenido y que un upgrade requiere llevar el código nuevo al workspace (re-copiar docker/+scripts/ desde el launcher actualizado o re-ejecutar el scaffold/--regenerate correspondiente) antes de docker compose build. |
| 3 | medium | stale | Imagen "agent-admin:latest" (diagrama de layout) y comandos "docker tag agent-admin:latest agent-admin:prev" / rollback _(Diagrama Host/Container (línea 43) y "Upgrade & Rollback" (líneas 160, 176))_ | setup.sh:1057: default docker_yaml es image_tag: "agentic-pod:latest"; modules/docker-compose.yml.tpl:6: image: {{DOCKER_IMAGE_TAG}} (por-agente desde agent.yml .docker.image_tag, schema.sh:45). "agent-admin:latest" no es el tag que produce el scaffold, así que el docker tag del upgrade falla. | Usar el tag real (default agentic-pod:latest, configurable vía agent.yml docker.image_tag) en el diagrama y en los comandos de upgrade/rollback. |
| 4 | medium | stale | "supervisor automation (heartbeat scheduling, plugin auto-install, qmd watcher, backups) is not ported to systemd in v1" _(Callout "Principle II trade-off (local mode)" (línea 19))_ | setup.sh:2419-2421 (agent-<name>-qmd-reindex.{service,timer} + qmd-watch.service), 2446-2447 (vault-backup.{service,timer}), 2470-2475 (wiki-graph.{service,timer}), 2611-2613 (_local_units incluye los 7). El qmd watcher y los backups SÍ están portados a systemd desde 012/014 — el propio doc lo describe en los párrafos 012/013/014 más abajo. Solo heartbeat scheduling y plugin auto-install siguen sin portar (no hay unidad heartbeat en _local_units). | Acotar la frase a lo que sigue sin portar (heartbeat scheduling y plugin auto-install) y remitir a los párrafos 012-014 para vault/QMD/backup/wiki-graph bajo systemd. |
| 5 | medium | needs-qualifier | "an unchanged vault skips the costly embed" (guard hash-debounced de qmd_reindex) _(Sección "Vault layer", párrafo 010-self-managing-rag (línea 281))_ | scripts/lib/qmd_index.sh:525-554 (018): el guard solo salta el embed si el vault no cambió Y pending==0; con pending>0 o desconocido reanuda ("vault unchanged …, embeddings pending — resuming"). Además el embed ya no es una pasada única: _qmd_embed_until_complete (líneas 484-519) itera hasta pending==0, stall o QMD_EMBED_MAX_PASSES (=12, línea 463), y qmd-index.json gana pending + estados partial/stalled (líneas 276-307). | Calificar: un vault sin cambios salta el embed solo si además está completamente embebido (pending==0); si quedan pendientes el guard reanuda con el loop multi-pasada de 018. |
| 6 | medium | stale | La entrada MCP del vault apunta a "npx @bitbonsai/mcpvault@latest /home/agent/.vault" _(Sección "Vault layer" (línea 277))_ | modules/mcp-json.tpl:69-71: args: ["-y", "@bitbonsai/mcpvault@0.12.0", "{{VAULT_MCP_PATH}}"] — versión PINNEADA a 0.12.0 (no @latest) y ruta resuelta por modo vía VAULT_MCP_PATH (no hardcodeada /home/agent/.vault). | Actualizar a la versión pinneada @0.12.0 y a la ruta mode-resolved {{VAULT_MCP_PATH}} (/home/agent/.vault solo en docker). |
| 7 | low | stale | Rutas "qmd_setup_if_needed (in docker/scripts/lib/qmd_index.sh)" y "inotify watcher (docker/scripts/qmd_watch.sh)" _(Sección "Vault layer", párrafo 010 (línea 281))_ | ls docker/scripts/lib/ → no contiene qmd_index.sh; canonical desde 012 es scripts/lib/qmd_index.sh y scripts/qmd_watch.sh (setup.sh:1542-1544 los define como fuente y 1562/1578 los espeja al docker/ del workspace; docker/Dockerfile:235,256 los hornea desde el contexto). El propio párrafo 012 del doc declara la relocación, contradiciendo estas rutas. | Citar las rutas canónicas scripts/lib/qmd_index.sh y scripts/qmd_watch.sh (espejadas al build context docker/ en el scaffold; /opt/agent-admin/scripts/ en la imagen). |
| 8 | low | stale | "The version pin is single-sourced from agent.yml … into both the .mcp.json entry and the lib" _(Sección "Vault layer", párrafo 010 (línea 281))_ | modules/mcp-json.tpl:73-76: la entrada qmd ya no contiene la versión (command=wrapper, args=[]); el wrapper resuelve el pin en runtime vía qmd_pkg() que lee agent.yml con default 2.5.3 (scripts/lib/qmd_index.sh:49-56, usado por qmd_mcp_exec en línea 259). | Decir que el pin sigue single-sourced en agent.yml pero fluye vía qmd_pkg() en la lib/wrapper; la entrada .mcp.json ya no embebe la versión (016). |
| 9 | low | stale | Diagrama de árbol de procesos: crond → "every 5 min: /workspace/scripts/heartbeat/heartbeat.sh" _(Sección "Process Tree Inside Container" (línea 65))_ | docker/entrypoint.sh:56: HEARTBEAT_CRON default "*/30 * * * *"; setup.sh:799-802: default del wizard 30m. El propio doc dice "default 30m" en la línea 129 — el diagrama se contradice con ambos. | Cambiar el diagrama a "every N min (default 30)" para alinearlo con el default real. |
| 10 | low | false | "vault_seed_if_empty (rsync skeleton + sed SCAFFOLD_DATE; no-op if dir non-empty)" _(Sección "Vault layer", diagrama de boot sequence (línea 273))_ | scripts/lib/vault.sh:44: usa cp -R "$skeleton"/. "$target"/, no rsync (el sed de SCAFFOLD_DATE sí existe, vault.sh:47). Detalle mecánico menor. | Cambiar "rsync skeleton" por "cp -R skeleton". |
| 11 | low | false | Enlace "Design Specification" a ../superpowers/specs/2026-04-18-agent-admin-docker-mode-design.md _(Sección "See Also" (línea 299))_ | ls superpowers/ en la raíz del repo → "No such file or directory"; la carpeta real es docs/superpowers/specs/ y solo contiene 2026-04-19-heartbeat-observability-cli-design.md y 2026-04-22-identity-backup-design.md — el archivo 2026-04-18-…-docker-mode-design.md no existe en ninguna de las dos rutas. | Eliminar el enlace o apuntarlo a un spec existente bajo docs/superpowers/specs/ (ruta relativa superpowers/specs/... desde docs/). |

**Coverage gaps**:
- 016 — Arquitectura del prefijo bun gestionado de QMD: _qmd_ensure_prefix bajo $QMD_CACHE_HOME/pkg con trustedDependencies (better-sqlite3, node-llama-cpp) y tree-sitter vía WASM; toolchain nativo del Dockerfile gateado por build-arg QMD_NATIVE_TOOLCHAIN; shim LD_PRELOAD bigstack.so (stack 8MB para std::regex en musl); y el par de wrappers MCP (docker /opt/agent-admin/scripts/qmd-mcp, local scripts/local/agent-qmd-mcp.sh) con {{QMD_MCP_COMMAND}} en .mcp.json.
- 017 — sqlite-vec en musl: el prebuilt glibc no carga en Alpine; docker/scripts/build-sqlite-vec.sh compila vec0.so musl en el build de la imagen y _qmd_swap_sqlite_vec lo swapea en el prefijo (solo docker/musl; no-op en glibc).
- 018 — Loop de completitud del embed: _qmd_embed_until_complete (pasadas frescas hasta pending==0, stall o QMD_EMBED_MAX_PASSES=12), guard reanudable del vault sin cambios, y el contrato ampliado de qmd-index.json (pending + last_status partial/stalled) — el doc aún describe el embed como una sola pasada con guard binario.
- 015 — TMPDIR/scratch respaldado por host bajo .state para qmd y wiki-graph (la sección Security aún presenta el tmpfs /tmp de 100MB como el único espacio escribible, sin mencionar que el pipeline RAG lo evita) y la lib espejada scripts/lib/rag_obs.sh (redact_secrets + scratch_dir, captura de stderr redactada).
- 015 — Endurecimiento del modo local: resolve_claude_bin (ruta absoluta del CLI en las unidades systemd, CLAUDE_BIN en systemd-remote-control.service.tpl) y _libc_variant para elegir build bun glibc/musl en el bootstrap — el bloque de modo local (011-014) no los menciona.

## docs/heartbeatctl.md

_docs/heartbeatctl.md is accurate for the docker-mode heartbeat core — interval divisor table, mutation transaction/rollback, exit codes, 15s sync loop and ~75s propagation, backup flags and default cadences, token-health dedup (TH_DEDUP_SECS=86400) and doctor thresholds (48h/25h/8d + DOCTOR_* env overrides) all verified against code. It has drifted in three ways: a few concrete false claims (manual `test` DOES bump consecutive_failures; `show` dumps the staging crontab, not /etc/crontabs/agent; the sync loop compares with cmp -s, not mtime; the probe table omits the claude_oauth expiry check), zero awareness of the local deployment mode added in 011-013, and a complete absence of `qmd-reindex` plus the 018 qmd-index.json pending/partial/stalled contract. Also worth noting outside the 011-019 window: `kick-channel` and `drop-plugin` (both listed in the script's own help, heartbeatctl:86-88) are missing from what NEXT_STEPS links as the "full reference (all subcommands)"._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | medium | false | `test` "Does **not** bump `counters.consecutive_failures` (a manual test must not poison the ok-streak)" _(Control commands > `test [--prompt "..."]` (line 68))_ | scripts/heartbeat/heartbeat.sh:325-337 — the new_counters jq expression increments consecutive_failures for any status error/timeout with no condition on $TRIGGER; docker/scripts/heartbeatctl:549-562 (cmd_test) only sets HEARTBEAT_TRIGGER=manual, which is recorded in the run line but never consulted by the counter update. A failed manual test DOES increment consecutive_failures (and total_runs), and a successful one resets it. | State that manual tests update counters exactly like cron ticks (trigger=manual is recorded in runs.jsonl but counters are trigger-agnostic). |
| 2 | medium | false | `show` dumps "`/etc/crontabs/agent` (what crond actually reads — root-owned)" _(Read commands > `show` (line 59))_ | docker/scripts/heartbeatctl:20 — CRONTAB_FILE defaults to `$HEARTBEAT_DIR/.crontab.staging`; cmd_show (heartbeatctl:145-146) cats $CRONTAB_FILE, i.e. the workspace staging file, never /etc/crontabs/agent. During the ≤75s sync window (or with a broken sync loop) `show` displays the desired schedule, not what crond is running — the exact diagnostic scenario the section claims to help with. | Say `show` dumps the staging crontab (`scripts/heartbeat/.crontab.staging`, the desired state) and that /etc/crontabs/agent may lag it by the propagation window. |
| 3 | medium | stale | Token health "Probed endpoints" table lists GITHUB_PAT, NOTIFY_BOT_TOKEN, ATLASSIAN_*_TOKEN as the probed set _(Token health > probed endpoints table (lines 252-257))_ | docker/scripts/check_token_health.sh:190-192 also runs `_check_one "claude_oauth" ... probe_claude_oauth`; docker/scripts/lib/token_health.sh:109-143 — a fourth probe reads `~/.claude/.credentials.json` expiresAt locally (no network call) and emits auth_fail when expired or expiring within 30 min, writing token-health/claude_oauth.json like the others. | Add a fourth row for the claude_oauth probe (local .credentials.json expiresAt check, no network call, early-warns 30 min before expiry). |
| 4 | medium | needs-qualifier | Doc frames heartbeatctl exclusively as in-container/docker ("Image-baked at /opt/agent-admin/..., docker exec -u agent ...") with no mention of the local deployment mode _(Intro + Invocation (lines 3-13), doc-wide)_ | scripts/agentctl:1193-1224 (cmd_local_heartbeat) — in local mode (011/012/013) only `qmd-reindex`, `backup-vault`, and `wiki-graph` have host equivalents (direct exec of scripts/local/agent-*.sh wrappers); every other `agentctl heartbeat <sub>` (including `token-check`) hits `_local_docker_only` and exits 2. modules/ contains local systemd units only for healthcheck/qmd-reindex/qmd-watch/vault-backup/wiki-graph — heartbeatctl itself is never installed on a local-mode host. | Add an invocation note: in local mode there is no heartbeatctl binary; only qmd-reindex/backup-vault/wiki-graph are available via `agentctl heartbeat`, everything else is docker-only. |
| 5 | low | needs-qualifier | Backup cadence table and token-health schedule are described purely as cron entries ("cron 0 * * * *", "cron 03:30", "Hourly cron") _(Backup commands table (lines 180-185) + Token health (lines 270-272))_ | Docker mode verified correct (heartbeatctl cmd_reload defaults: identity '30 3 * * *' line 233, vault '0 * * * *' line 245, config '30 3 * * *' line 258, token-health '0 * * * *' line 272). But in local mode the vault-backup/qmd/wiki-graph cadences run as systemd timers via OnCalendar (modules/local-vault-backup.timer.tpl etc., feature 012), and identity backup, config backup, and token health have NO local scheduler at all (no local-* unit templates exist for them; agentctl cmd_local_heartbeat rejects token-check). | Qualify each row with its mode: docker=cron via heartbeatctl reload; local=vault-backup timer only (OnCalendar), identity/config/token-health docker-only. |
| 6 | low | false | Propagation step 2: the sync loop "notices the staging file is newer than /etc/crontabs/agent" _(Propagation timing (line 154))_ | docker/entrypoint.sh:70 ("Comparison uses byte-identity (cmp -s), not mtime") and :85 (`! cmp -s "$STAGING_CRONTAB" "$CRONTAB_DST"`). The loop copies on content difference, not mtime; a content-identical touch never triggers a copy. The 15s interval (entrypoint.sh:83 `sleep 15`) and ~75s worst case are correct. | Replace "is newer than" with "differs byte-for-byte from (cmp -s)". |
| 7 | low | needs-qualifier | `status --json` "emits `state.json` verbatim" _(Read commands > `status [--json]` (line 38))_ | docker/scripts/heartbeatctl:338-348 (_enrich_state) + :359-364 — the output is state.json re-serialized through `jq -c` with a live `.crond = {alive, pid}` overlay injected at read time, so it is not byte-verbatim and contains a field state.json itself never carries populated (heartbeat.sh:380 writes crond:{alive:null,pid:null}). | Say it emits state.json enriched with a live .crond {alive,pid} overlay (machine-parseable, not byte-identical to the file). |
| 8 | low | stale | "Data files — All under /workspace/scripts/heartbeat/" lists only heartbeat.conf, state.json, .crontab.staging, runs.jsonl, cron.log, sessions/ _(Data files (lines 161-170))_ | docker/scripts/heartbeatctl:61-69 — the same directory also holds identity-backup.json, vault-backup.json, config-backup.json, token-health/*.json (+warnings.jsonl, check_token_health.sh:20), qmd-index.json (018 schema {hash,last_run,last_status,runs[,pending]}, scripts/lib/qmd_index.sh:276-307), wiki-graph.json and .wiki-graph.lock; cmd_reload lines 235/247/260/274/287/300 add logs/backup-*.log, token-health.log, qmd-reindex.log, wiki-graph.log. | Extend the list with the per-primitive state files (identity/vault/config-backup.json, token-health/, qmd-index.json, wiki-graph.json) and their log files. |

**Coverage gaps**:
- `qmd-reindex` subcommand is entirely undocumented (the Vault RAG section covers only wiki-graph): its --dry-run flag, opt-in via vault.qmd.enabled, */5 cron backstop + vault.qmd.schedule override, flock-guard + hash-debounce, and the docker/local invocation split (heartbeatctl:948-996; cmd_reload:282-288; agentctl:1198-1207 — local wrapper refuses --dry-run).
- 018 embed-completion semantics: the multi-pass embed loop `_qmd_embed_until_complete` (QMD_EMBED_MAX_PASSES=12, env-overridable for tests only), the resumable unchanged-vault guard (skip only when hash unchanged AND pending==0 — an absent pending counts as unknown and resumes), and the qmd-index.json state schema {hash, last_run, last_status: indexed|error|partial|stalled, runs, pending} (scripts/lib/qmd_index.sh:276-307, 463-525).
- Local deployment mode (011/012/013) operation map for this CLI's surface: which actions have host equivalents (`agentctl heartbeat qmd-reindex|backup-vault|wiki-graph` exec scripts/local/agent-*.sh directly, no systemctl), that schedules become systemd OnCalendar timers instead of cron, and that the local kill-switch (013 FR-008, modules/local-killswitch.sh.tpl) stops the qmd/vault-backup/wiki-graph timers documented here alongside the main unit.
- Heartbeat-session config-dir isolation detail changed post-freeze: the doc's "no plugins by default" note (line 129) predates the current mechanism where settings.json is rewritten with enabledPlugins={} and plugins/ is an empty dir rather than symlinked (heartbeat.sh:109-166) — worth a one-line refresh if the prompt-authoring guidance stays.

## docs/vault.md

_The vault core of this doc (Karpathy layers, seeding, force_reseed, coexistence heuristics, and the feature-014 wiki-graph section) is accurate against current code, but the entire QMD half is frozen at the pre-010/pre-016 era: it documents a manual, bunx-driven, @latest pipeline that no longer exists — setup and reindex are now fully launcher-managed (boot setup, */5 cron, inotify watcher, 018 multi-pass embed), qmd runs from a pinned managed bun prefix, and the MCP entry renders per-mode wrappers instead of bunx. Additionally the doc is written docker-only and never acknowledges the local/systemd deployment mode (011/012), so several of its commands are a broken flow for local-mode operators. Fifteen findings (4 high) and nine coverage gaps, all verified against mcp-json.tpl, qmd_index.sh, setup.sh, heartbeatctl, start_services.sh, vault.sh, backup_vault.sh, the local-*.tpl modules, and the Dockerfile._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | When QMD is enabled, .mcp.json carries a qmd entry: "command": "bunx", "args": ["@tobilu/qmd@latest", "mcp"], "env": {} _(Hybrid search via QMD, lines 308-316)_ | modules/mcp-json.tpl:72-77 renders the qmd server as command={{QMD_MCP_COMMAND}}, args=[], env={{QMD_MCP_ENV}}; setup.sh:2018-2022 resolves QMD_MCP_COMMAND to /opt/agent-admin/scripts/qmd-mcp (docker) or <ws>/scripts/local/agent-qmd-mcp.sh (local); docker/scripts/qmd-mcp exists and qmd_index.sh:252-268 (qmd_mcp_exec) execs qmd from the managed prefix, explicitly NOT bunx (T036/BUG 4 comment). | Replace the JSON block with the rendered per-mode wrapper contract (command={{QMD_MCP_COMMAND}}, args=[], env={{QMD_MCP_ENV}}) and name the two wrappers. |
| 2 | high | false | QMD is not auto-configured at boot; the first time you use it, run 'bunx @tobilu/qmd@latest collection add / update / embed' manually inside the container (one-time per agent) _(One-time setup inside the container, lines 322-339)_ | scripts/lib/qmd_index.sh:322-360 qmd_setup_if_needed does collection add --mask '**/*.md' → update → embed idempotently (sentinel .qmd-setup-ok), and docker/scripts/start_services.sh:170-173 backgrounds it at every boot when vault.qmd.enabled; also the shown bunx invocations are the exact pattern feature 016 removed (qmd_index.sh:92-94: managed bun prefix, not bunx) — running bunx by hand recompiles tree-sitter and aborts on Alpine musl (BUG 4). | Document that setup is automatic at boot (backgrounded qmd_setup_if_needed) and replace the bunx commands with 'agentctl heartbeat qmd-reindex' / heartbeatctl qmd-reindex for manual triggering. |
| 3 | high | false | QMD does not watch the vault; re-index by running bunx update/embed manually, and automating it via cron is 'out of scope for the launcher — agents that need it can wire it themselves' _(Re-indexing, lines 355-365)_ | docker/scripts/heartbeatctl:278-287 renders a managed cron line from vault.qmd.schedule (default */5 * * * *) calling 'heartbeatctl qmd-reindex'; scripts/qmd_watch.sh is the inotify watcher (heartbeatctl:126 and 944 reference it); local mode ships modules/local-qmd-reindex.{service,timer}.tpl and local-qmd-watch.{service,sh}.tpl; setup.sh:1200-1202 writes vault.qmd.schedule into agent.yml at scaffold. | Rewrite the section: reindex is launcher-managed (flock-guarded, hash-debounced qmd_reindex) via the */5 cron backstop + inotify watcher in both modes, with cadence override via vault.qmd.schedule. |
| 4 | high | needs-qualifier | Every operational instruction assumes docker (docker exec -u agent, docker compose restart/build, container paths like /home/agent/.vault, '~/.vault/CLAUDE.md inside the container') _(Whole doc (Where it lives, Lifecycle, Troubleshooting, QMD sections))_ | Feature 011/012 added deployment.mode docker\|local: setup.sh:2295 _seed_vault_local seeds the vault host-side in local mode; modules/local-{qmd-reindex,qmd-watch,vault-backup,wiki-graph}.{service,timer,sh}.tpl provide systemd equivalents; setup.sh:1987-1992 resolves VAULT_MCP_PATH per mode (/home/agent/.vault vs $LOCAL_VAULT_DIR); modules/local-qmd-reindex.sh.tpl:39-41 sets XDG_CACHE_HOME/QMD_CONFIG_DIR/QMD_CACHE_HOME under <ws>/.state for local storage. | Add a per-mode note up front and local-mode command equivalents (systemctl/agentctl instead of docker exec/compose) wherever a docker command appears. |
| 5 | medium | stale | 'Why bunx and not npx': the Alpine 3.20 base image ships Node 20 (below QMD's floor), Bun 1.1.38 is already installed, 'so we route QMD through bunx' _(Hybrid search via QMD, lines 318-321)_ | docker/Dockerfile:5 ARG BASE_IMAGE=alpine:3.24.1 (not 3.20); Dockerfile:129 ARG BUN_VERSION=1.3.14 (not 1.1.38); and qmd is no longer routed through bunx at all — qmd_index.sh:92-103 installs it into a managed bun prefix ($QMD_CACHE_HOME/pkg) with trustedDependencies limited to better-sqlite3 + node-llama-cpp (016). | Replace the rationale with the 016 managed-prefix contract (bun install into ~/.cache/qmd/pkg, native deps compiled with the image toolchain) and drop the outdated version numbers. |
| 6 | medium | stale | .mcp.json vault entry is "args": ["-y", "@bitbonsai/mcpvault@latest", "/home/agent/.vault"] _(MCP integration (MCPVault), lines 253-259)_ | modules/mcp-json.tpl:68-71: args are ["-y", "@bitbonsai/mcpvault@0.12.0", "{{VAULT_MCP_PATH}}"] — the version is pinned and the path is mode-resolved (setup.sh:1987-1992). | Show the pinned @0.12.0 spec and note the vault path renders per deployment mode via VAULT_MCP_PATH. |
| 7 | medium | false | npx caches the MCPVault package under /home/agent/.npm/ (also bind-mounted via .state/.npm/); troubleshooting says to fix cache corruption with rm -rf /home/agent/.npm _(MCP integration lines 268-269 and Troubleshooting line 462)_ | docker/Dockerfile:193-208: NPM_CONFIG_CACHE=/opt/npm-cache with NPM_CONFIG_PREFER_OFFLINE=true and a build-time pre-warm — the cache was moved OFF the /home/agent bind-mount in feature 004 (VirtioFS errno -35). | Point both passages at /opt/npm-cache (image-baked, pre-warmed; clearing it requires an image rebuild or exec as the owning UID). |
| 8 | medium | stale | All example qmd commands use @tobilu/qmd@latest _(One-time setup / Re-indexing / .mcp.json blocks, lines 311, 329-335, 359-360)_ | setup.sh:1200-1201 writes vault.qmd.version: "2.5.3" into agent.yml and qmd_index.sh:49-57 (qmd_pkg) single-sources the pin from there; the pin is a guardrail paired with sqlite-vec 0.1.9 (qmd_index.sh:112, docker/Dockerfile ARG SQLITE_VEC_VERSION). | State that the version is pinned in agent.yml vault.qmd.version (2.5.3) and must only be bumped via docs/qmd-upgrade-checklist.md. |
| 9 | medium | needs-qualifier | Storage table: ~/.cache/qmd/{index.sqlite,models/} ↔ <workspace>/.state/.cache/qmd/... is the whole QMD storage story _(Storage and cost, lines 341-352)_ | Correct for docker (bind-mount makes ~/.cache = .state/.cache), but local mode reaches the same host paths via explicit env (modules/local-qmd-reindex.sh.tpl:39-41: XDG_CACHE_HOME=<ws>/.state/.cache, QMD_CONFIG_DIR=<ws>/.state/.config/qmd, QMD_CACHE_HOME=<ws>/.state/.cache/qmd), and the table omits two post-016 residents: the managed install prefix ~/.cache/qmd/pkg (qmd_index.sh:103) and the QMD_CONFIG_DIR collections config. | Add the pkg/ prefix and .state/.config/qmd rows and a note that local mode pins the same locations via XDG_CACHE_HOME/QMD_CONFIG_DIR exports in the wrappers. |
| 10 | low | stale | The wizard's Knowledge vault section asks three opt-in questions (defaults yes) _(Lifecycle → At scaffold, line 75)_ | setup.sh:818-826: four questions — enable vault (y), seed (y), MCPVault (y), plus 'Enable QMD hybrid search…' whose default is "n". | Say four questions and note the QMD one defaults to no. |
| 11 | low | stale | agent.yml vault block example (enabled/path/seed_skeleton/initial_sources/mcp/schema) _(Lifecycle → At scaffold, lines 86-98)_ | setup.sh:1190-1205 also writes force_reseed: false and a qmd: block (enabled, version: "2.5.3", schedule: "*/5 * * * *"); the doc's separate qmd enable example (lines 293-299) also omits version/schedule. | Update the YAML example to include force_reseed and the full qmd block with version and schedule. |
| 12 | low | stale | File reference tree lists six wiki/ subdirectories and seven _templates/ files, plus .obsidian/ _(File reference, lines 224-247)_ | ls -R modules/vault-skeleton: wiki/ now has a seventh subdir normalization/ and _templates/ includes normalization.md (both added by 014); .obsidian/ still present (ls -la confirms). The tree contradicts the doc's own feature-014 section that documents wiki/normalization/. | Add wiki/normalization/ and _templates/normalization.md to the tree. |
| 13 | low | false | vault_seed_if_empty rsyncs /opt/agent-admin/modules/vault-skeleton/ into the vault _(Lifecycle → At first container boot, step 4, lines 108-110)_ | scripts/lib/vault.sh:44: the copy is cp -R "$skeleton"/. "$target"/ — no rsync involved. | Say 'copies (cp -R)' instead of 'rsyncs'. |
| 14 | low | stale | If a new MCPVault version breaks compatibility: pin a version in modules/mcp-json.tpl by replacing @latest with @<version> _(Troubleshooting → npx @bitbonsai/mcpvault fails, lines 463-464)_ | modules/mcp-json.tpl:70 is already pinned to @bitbonsai/mcpvault@0.12.0 — there is no @latest to replace. | Invert the advice: the version is pinned in the template; bump it there deliberately if an upgrade is needed. |
| 15 | low | unverified-suspicion | The qmd MCP server exposes the tools query, get, multi_get, status _(After step 3 (line 338-339) and 'MCP tools that QMD exposes' (lines 380-386))_ | No in-repo reference confirms these MCP tool names for the pinned qmd 2.5.3 (grep for multi_get/deep_search across specs/ and tests/ returned nothing; qmd_index.sh:16-20 documents only the CLI verbs). The list predates the 2.5.3 pin (feature 010) and may have drifted with the package; verifying would require running the pinned package's MCP server, which I did not do. | Re-verify the tool list against @tobilu/qmd@2.5.3's MCP server output and update or drop the enumeration. |

**Coverage gaps**:
- Local/systemd deployment mode (011/012): vault location and seeding in local mode (setup.sh::_seed_vault_local), the agent-<name>-{qmd-reindex,qmd-watch,vault-backup,wiki-graph} systemd timers/units, and non-docker command equivalents for every operation the doc describes
- The self-managing QMD pipeline (010/012/013): boot-time qmd_setup_if_needed, the flock-guarded hash-debounced qmd_reindex, the */5 cron backstop (vault.qmd.schedule), the inotify watcher (qmd_watch.sh / agent-qmd-watch), the qmd-index.json state file, and heartbeatctl/agentctl qmd-reindex
- QMD version pinning and upgrade path: agent.yml vault.qmd.version (2.5.3), the qmd↔sqlite-vec pairing guardrail, and docs/qmd-upgrade-checklist.md
- 016 invocation contract: managed bun-install prefix at ~/.cache/qmd/pkg (trustedDependencies better-sqlite3 + node-llama-cpp), the qmd MCP wrappers (/opt/agent-admin/scripts/qmd-mcp and scripts/local/agent-qmd-mcp.sh), and the QMD_MCP_COMMAND/QMD_MCP_ENV render variables
- 017 musl sqlite-vec: on docker/Alpine, vector embed requires the image-baked vec0.so (QMD_NATIVE_TOOLCHAIN build-arg); the degraded 'vector embed unavailable, lexical index intact' mode when it is absent
- 018 embed completion: the multi-pass embed loop (_qmd_embed_until_complete, QMD_EMBED_MAX_PASSES=12), the pending field and partial/stalled/indexed statuses in qmd-index.json, and the resume-instead-of-skip guard for unchanged vaults with pending embeddings
- Vault backup: the backup/vault orphan branch (markdown-only staging per backup_vault.sh, exclude patterns, hourly default, vault.backup_schedule override) — the doc mentions '.graph is excluded from the vault backup' but never documents the backup itself
- 013 ops parity touchpoints for the vault/QMD subsystem: kill-switch stops qmd+backup, agentctl doctor exit 0/1/2 contract, and agentctl status vault/qmd subsystem lines
- 014 config keys for the graph runner the doc already describes: vault.wiki_graph.enabled and vault.wiki_graph.schedule (default 20 */6 * * *)

## docs/state-layout.md

_docs/state-layout.md (last touched Apr 27, commit 9f11f01) is accurate at its docker-mode core — the two bind-mounts, tmpfs 100m, heartbeat isolation dir, uninstall matrix, --purge scope, log rotation, and default image tag agentic-pod:latest all still match the code. However it predates features 011-018 entirely: it is silently docker-only (no local/systemd mode), its gitignore column is wrong for agent.yml/CLAUDE.md/.mcp.json (all three ARE gitignored, with 'commit it' advice that would silently fail), and it omits the whole new state surface — qmd index/prefix/scratch under .state/.cache, wiki-graph .graph/ artifacts, fork-backup state files and clone caches, .gcal creds, and local-mode secrets/units. The telegram patched-marker count (8) and the vault layout (six wiki subdirs, no .graph/) are also stale._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | Table 'Workspace-level files': agent.yml — Gitignored? 'no (commit it)' _(Section 'Workspace-level files (outside .state/)', table row agent.yml (line 242))_ | .gitignore:3 has '/agent.yml' under '# Files generated by setup.sh (ignored from version control)'; setup.sh:1802 copies this same .gitignore into every scaffolded workspace; setup.sh:395 help text: 'agent.yml  Source of truth (user-owned, gitignored by default)'. Durability is instead provided by the backup/config orphan branch (docker/scripts/lib/backup_config.sh). | Mark agent.yml as gitignored and point to the backup/config orphan-branch mechanism (and --restore-from-fork) as the durability path instead of 'commit it'. |
| 2 | high | needs-qualifier | Doc maps 'every piece of agent state to a concrete path on the host AND inside the container'; all tables assume the two bind-mounts and /home/agent paths _(Intro (lines 3-5) and every Host\|Container table)_ | Since feature 011 agent.yml has deployment.mode docker\|local. In local mode there is no container: setup.sh:1992-1995 resolves VAULT_MCP_PATH=$LOCAL_VAULT_DIR and GCAL_CREDS_PATH=$SCRIPT_DIR/.state/.gcal/gcp-oauth.keys.json; setup.sh:2009 sets qmd MCP env XDG_CACHE_HOME=$SCRIPT_DIR/.state/.cache, QMD_CONFIG_DIR=$SCRIPT_DIR/.state/.config/qmd; local mode adds .state/remote-control.env (setup.sh:2232), scripts/local/ helpers (setup.sh:2234-2242) and agent-*.{service,timer} units (.gitignore:26-30); uninstall removes 10 systemd units (setup.sh:2607-2614). | Add an upfront 'deployment modes' note stating the Container column applies only to deployment.mode=docker, and that in local mode the same .state/ subtree is used directly under the workspace with no /home/agent mapping. |
| 3 | medium | false | Table rows CLAUDE.md and .mcp.json — Gitignored? 'no' _(Section 'Workspace-level files (outside .state/)', rows CLAUDE.md (line 245) and .mcp.json (line 249))_ | .gitignore:2 '/CLAUDE.md' and .gitignore:5 '/.mcp.json' — both listed as setup.sh-generated ignored files; this .gitignore is copied into the workspace by setup.sh:1802. | Flip both rows to gitignored=yes (they are derived files regenerable via setup.sh --regenerate). |
| 4 | medium | stale | grep -c "agentic-pod-launcher:" server.ts should return 8 (1 typing + 4 offset hunks + 1 stderr + 2 primary) _(Section 'Plugins — installed cache and patched code' (line 159))_ | docker/scripts/apply_telegram_typing_patch.py: typing patch is now v4 (line 61 MARKER_TYPING = 'typing refresh patch v4') and inserts TWO colon-marker strings — the group marker (line 107) plus the inline '_typingStop(chat_id) // agentic-pod-launcher: stop typing refresh' (line 485) — alongside 4 offset (lines 177,212,222,227), 2 primary (232,243), 1 stderr (250) = 9 total; a v1→v4 upgrade cascade runs every boot. | Update the count to 9 (2 typing + 4 offset + 1 stderr + 2 primary) and note the typing marker is versioned (currently v4) so exact-count greps are fragile across patch upgrades. |
| 5 | medium | stale | Layer C layout: 'wiki/ ... Six type subdirs verbatim from Karpathy' (summaries/entities/concepts/comparisons/overviews/synthesis) _(Section 'Layer C — knowledge vault', layout block (lines 87-93))_ | find modules/vault-skeleton -type d shows a seventh subdir wiki/normalization/ (added by feature 014); scripts/lib/wiki_graph.sh:162-185 validates normalization/ pages (canonical + aliases contract). | Add wiki/normalization/ (canonical-name/alias pages, feature 014) as the seventh subdir. |
| 6 | medium | stale | Layer C vault layout omits any .graph/ directory (tree ends at .obsidian/) _(Section 'Layer C — knowledge vault', layout block (lines 83-99))_ | scripts/lib/wiki_graph.sh:10 'Artifacts land under <vault>/.graph/{graph,backlinks,findings}.json'; :73 wiki_graph_dir() prints "$1/.graph"; :365-373 atomic writes of graph.json, backlinks.json, findings.json; runner state at /workspace/scripts/heartbeat/wiki-graph.json (:78). | Add <vault>/.graph/{graph,backlinks,findings}.json (derived, regenerable wiki-graph artifacts, feature 014) to the layout. |
| 7 | medium | needs-qualifier | .state/.cache is a 'generic XDG cache dir' and these caches can be wiped safely — 'the next boot rebuilds whatever it needs' _(Section 'Other state under .state/ (cache + runtime)' (lines 256-264))_ | scripts/lib/qmd_index.sh:62 qmd_cache_root=$HOME/.cache/qmd (→ .state/.cache/qmd), :103 managed bun-install prefix at $(qmd_cache_root)/pkg with .installed-hash (:197-210); scripts/lib/rag_obs.sh:26-37 host-backed scratch at $BASE/tmp; scripts/lib/backup_vault.sh:132 and docker/scripts/lib/backup_config.sh:47 clone caches at /home/agent/.cache/agent-backup. Wiping .cache/qmd discards the semantic index — rebuilt only by a full reindex + multi-pass embed (expensive on large vaults per 018), not automatically at boot. | Qualify that .state/.cache now contains the qmd index + managed bun prefix (.cache/qmd/{pkg,tmp}) and backup clone caches (.cache/agent-backup/), all regenerable but expensive — wiping .cache/qmd forces a full re-embed. |
| 8 | low | needs-qualifier | Vault host path fixed at <workspace>/.state/.vault/ _(Section 'Layer C — knowledge vault', Host\|Container table (line 77))_ | scripts/lib/schema.sh:80 declares '.vault.path' as a schema key; docker/scripts/start_services.sh:77-86 reads vault.path (default .state/.vault) and maps non-default paths to /home/agent/${vault_path#.state/}; setup.sh:1981 resolves LOCAL_VAULT_DIR from the same key. | Note .state/.vault is the default of the configurable agent.yml vault.path key. |
| 9 | low | stale | The vault skeleton is 'rsynced into /home/agent/.vault on first container boot if vault.seed_skeleton is true' _(Section 'Layer C — knowledge vault' (line 120))_ | scripts/lib/vault.sh:45 vault_seed_if_empty uses 'cp -R "$skeleton"/. "$target"/', not rsync; additionally since 014 every boot also runs vault_seed_missing (vault.sh:70+) — an additive delta upgrade for pre-populated vaults (hidden sentinel _templates/.schema-updates-0.8.0.applied). | Say 'copied (cp -R)' and mention the additive vault_seed_missing upgrade pass that runs on every boot for already-populated vaults. |
| 10 | low | stale | 'The launcher's default catalog (5 plugins) populates two marketplaces' with superpowers as the only opt-in shown _(Section 'Plugins — installed cache and patched code' (lines 141-157))_ | modules/plugins/ now holds 10 declarative descriptors: 5 type=default (claude-md-management, claude-mem, context7, security-guidance, telegram) and 5 type=optional (code-simplifier, commit-commands, github, skill-creator, superpowers), read via scripts/lib/plugin-catalog.sh. The 5-default count still holds, but 4 more optional plugins can appear in the cache tree. | State the catalog is descriptor-driven (modules/plugins/*.yml, 5 default + 5 optional) so the cache tree varies with opt-ins. |
| 11 | low | needs-qualifier | /tmp (tmpfs, 100 MB) includes the watchdog markers 'and any temp files from runtime tools' _(Section 'What does NOT persist' (line 270))_ | tmpfs 100m confirmed (modules/docker-compose.yml.tpl:37) and /tmp/agent-watchdog confirmed (docker/scripts/start_services.sh:242), but since 015 qmd/bun/wiki-graph temp is redirected to a host-backed scratch under .state: rag_obs.sh:26-37 scratch_dir → $BASE/tmp, and qmd_index.sh:209,248,265 export TMPDIR/TMP/TEMP to it — so RAG temp files persist under .state/.cache/qmd/tmp instead of the tmpfs. | Qualify that RAG tooling (qmd install/embed, wiki-graph) uses a persistent host-backed TMPDIR under .state/.cache/qmd/tmp, not the tmpfs. |

**Coverage gaps**:
- Local deployment mode (011/012): state map for mode=local — .state/remote-control.env and .state/healthcheck-notify.env (secrets, .gitignore:21-23), rendered scripts/local/ helpers (agent-login/killswitch/healthcheck/bootstrap/qmd-*/qmd-mcp, setup.sh:2232-2242), staged agent-*.{service,timer} units plus their installed copies in /etc/systemd/system (removed by uninstall, setup.sh:2607-2614), and workspace-direct paths with no /home/agent mapping
- qmd RAG storage (010/013/016/017/018): .state/.cache/qmd/ (index sqlite, managed bun-install prefix pkg/ + .installed-hash, scratch tmp/), .state/.config/qmd (QMD_CONFIG_DIR in local mode), the qmd MCP server entry in .mcp.json (command wrapper /opt/agent-admin/scripts/qmd-mcp or scripts/local/agent-qmd-mcp.sh), and reindex state scripts/heartbeat/qmd-index.json {hash,last_run,last_status,runs[,pending]} incl. 018's partial/stalled statuses (qmd_index.sh:276-295,510-519)
- Wiki-graph state (014): derived artifacts <vault>/.graph/{graph,backlinks,findings}.json plus runner state scripts/heartbeat/wiki-graph.json (wiki_graph.sh:10,78)
- Fork-backup state: per-primitive state files scripts/heartbeat/{identity,vault,config}-backup.json (heartbeatctl:61-63), clone caches .state/.cache/agent-backup/{identity,vault,config}-clone/ in docker vs ~/.cache/agent-backup in local mode (backup_vault.sh:132; local vault-clone removed by --purge, setup.sh:2684-2687), and setup.sh --restore-from-fork as a lifecycle command in the lifecycle table (setup.sh:418,1676)
- Google Calendar MCP OAuth credentials at .state/.gcal/gcp-oauth.keys.json (/home/agent/.gcal/ in docker; setup.sh:1990,1995) — secret material under .state not listed in any table
- Crontab-driven state beyond the heartbeat: /etc/crontabs/agent now carries up to 7 lines (heartbeat, identity/vault/config backups, hourly token-health probe, qmd reindex backstop, wiki-graph backstop — heartbeatctl:229-301), with associated state dir scripts/heartbeat/token-health/ and logs (token-health.log, and the qmd/wiki-graph cron logs) missing from the 'Heartbeat artifacts' table

## docs/adding-an-mcp.md

_docs/adding-an-mcp.md dates from the initial import (single commit 9ede904, never touched) and is doubly broken: its body is a "Docker Mode" supplement that defers all core steps to a generic "Adding an MCP" guide that turns out to be a self-link to itself, and its Docker-specific mechanics predate nearly everything current — it describes a named-volume state model (now a .state bind-mount), a Debian base-image escape hatch that cannot build (apk-only Dockerfile; 016/017 solved musl natives in-image instead), image-baked templates (they render host-side into the workspace), /home/agent/.claude-personal paths that exist nowhere, and a secrets-in-agent.yml example that would leak tokens to the plaintext config backup branch. It also predates the MCP catalog (modules/mcps/ descriptors), deployment modes (011), and the per-mode qmd/gcal render variables, so its "Standard Template Setup" teaches an obsolete flow; 6 high-severity findings and a near-total rewrite around the catalog + per-mode contract are warranted._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | "Follow [Adding an MCP](adding-an-mcp.md) as the primary reference" — defers all template/wizard integration steps to a generic guide _(Standard Template Setup, line 7)_ | The link is a self-reference: the file IS docs/adding-an-mcp.md, yet its body is titled 'Adding an MCP (Docker Mode)'. ls docs/ shows no other MCP guide, and `git log --follow docs/adding-an-mcp.md` shows a single commit (9ede904, initial import) — the promised generic doc never existed in this repo. | Replace the self-link with the actual integration flow (catalog descriptor in modules/mcps/ + mcp-json.tpl block) documented inline or in a real companion doc. |
| 2 | high | stale | 4-step flow: update the wizard (setup.sh) to prompt, update agent.yml, update mcp-json.tpl, update env-example.tpl for placeholder secrets _(Standard Template Setup, lines 9-12)_ | Adding an MCP is now catalog-driven: setup.sh:684-746 loops `mcp_catalog_list optional` over declarative descriptors in modules/mcps/<id>.yml (9 exist, e.g. google-calendar.yml with spec/runtime/secret_env_var/post_install_note), auto-prompts, exports MCPS_<ID>_ENABLED (scripts/lib/mcp-catalog.sh:75-78), writes the secret directly into .env (setup.sh mcp_secret_env_lines), and persists mcps.defaults to agent.yml. modules/env-example.tpl carries only Atlassian/GitHub blocks (grep: lines 14, 23) — catalog MCP secrets never touch it. Only the {{#if MCPS_<ID>_ENABLED}} block in mcp-json.tpl remains a manual edit. | Rewrite the steps as: (1) add modules/mcps/<id>.yml descriptor, (2) add the gated block to modules/mcp-json.tpl, (3) update the three test touchpoints — no setup.sh or env-example.tpl edits for standard cases. |
| 3 | high | false | "These files are baked into the image at build time" (referring to setup.sh, agent.yml, mcp-json.tpl, env-example.tpl), and the Building and Testing section applies template edits via `docker compose build` _(Standard Template Setup line 14 + Building and Testing lines 140-149)_ | Templates are rendered host-side: setup.sh:2184 `render_to_file "$modules_dir/mcp-json.tpl" "$SCRIPT_DIR/.mcp.json"`; the rendered .mcp.json reaches the container via the ./:/workspace bind-mount (modules/docker-compose.yml.tpl:55). docker/Dockerfile COPY list bakes modules/mcps/ descriptors (line 287) and admin scripts, but never mcp-json.tpl or setup.sh. `docker compose build` alone will never surface an edited MCP template — `./setup.sh --regenerate` is the re-render step (setup.sh:2138, 2184). | State that templates render host-side into workspace files reached via the bind-mount, and that the flow after a template edit is `./setup.sh --regenerate` (image rebuild only for docker/ changes). |
| 4 | high | false | Home-scope configuration lives at /home/agent/.claude-personal/... (installed_plugins.json, .mcp-auth, .codex) "inside the named volume"; persistence guidance says write to /home/agent/ "(the named volume)" / check "the state volume" _(File Paths Inside the Container lines 29-38 + State not persisting lines 183, 157)_ | (a) /home/agent is a bind-mount of <workspace>/.state, not a named volume: modules/docker-compose.yml.tpl:60 `./.state:/home/agent` (comment: immune to `docker compose down -v`). (b) The container config dir is /home/agent/.claude: docker/scripts/start_services.sh:228 `CLAUDE_CONFIG_DIR_VAL="/home/agent/.claude"`. Grep for `.claude-personal\|.mcp-auth\|.codex\|installed_plugins` across *.sh/*.tpl/Dockerfile finds zero container-side references — only a host-side legacy fallback in setup.sh:2150-2152 for the operator's own machine. | Replace the path list with /home/agent/.claude (config dir) plus the .state/ bind-mount model, pointing to docs/state-layout.md. |
| 5 | high | false | If an MCP's native binary needs glibc, set `docker.base_image: "debian:13-slim"` in agent.yml and rebuild; "Debian images... support most pre-built binaries" (also the apt-get RUN example and 'use the Debian variant' in Common Issues) _(Alpine musl vs. glibc, lines 44-82 + lines 174, 179)_ | docker.base_image exists and feeds FROM via build arg (modules/docker-compose.yml.tpl:17, docker/Dockerfile:5-6), but the Dockerfile is Alpine-only: `RUN apk add --no-cache` at Dockerfile:27, 66, 131-132 fails on any Debian base, so the documented switch cannot build. setup.sh only ever writes/backfills alpine values (setup.sh:1060 `base_image: "alpine:${_v_alpine}"`, 1930-1935). The musl native-module problem is instead solved in-image since 016/017: QMD_NATIVE_TOOLCHAIN build-arg toolchain (Dockerfile:66), bigstack.so, and build-sqlite-vec.sh musl compile (Dockerfile:63-64). | Delete the Debian-variant instructions and document the real recipe: keep Alpine, add build deps behind a build-arg (016 pattern) or compile the native module for musl at image build (017 pattern). |
| 6 | high | false | Multi-instance example models `token: "..."` per row inside agent.yml's mcps array _(Multi-Instance MCPs, lines 114-123)_ | The real contract keeps secrets out of agent.yml: tests/fixtures/sample-agent.yml:57-61 shows atlassian rows with name/url/email only; the token lives in .env as ${ATLASSIAN_<NAME>_TOKEN} referenced from mcp-json.tpl:53-58. agent.yml is pushed in plaintext to the fork's backup/config orphan branch (per docs/architecture.md backup model), so a token stored there would be exfiltrated to the remote. | Drop `token:` from the agent.yml example and show the secret flowing through .env with a ${SERVICE_TOKEN_{{NAME}}} reference, noting agent.yml is backed up in plaintext. |
| 7 | medium | false | "you can rotate secrets without rebuilding: nano .env; docker compose restart" _(Environment Variables, lines 102-108)_ | env_file is injected at container creation (modules/docker-compose.yml.tpl:67-68); `docker compose restart` restarts the existing container without recreating it, and Docker's documented restart semantics state configuration changes are not applied — so the container keeps the old env values. Container boot scripts only re-read /workspace/.env for TELEGRAM_BOT_TOKEN sync (start_services.sh:418), not for MCP secrets. Rotation requires `docker compose up -d` (recreate). Not executed live in this audit; mechanism verified from the compose template and Docker's documented restart contract. | Change the command to `docker compose up -d` (container recreate) and note that `restart` does not re-read env_file. |
| 8 | medium | stale | "MCPs in Docker mode use the same template structure as host mode" _(Intro, line 3)_ | There is no 'host mode': scripts/lib/schema.sh _SCHEMA_ENUMS pins '.deployment.mode=docker,local' (011). mcp-json.tpl is now mode-branched per entry: {{#if DEPLOYMENT_MODE_IS_DOCKER}}/workspace{{/if}}{{#unless}}{{DEPLOYMENT_WORKSPACE}}{{/unless}} (lines 9, 13), per-mode {{GCAL_CREDS_PATH}} (line 34) and {{QMD_MCP_COMMAND}}/{{QMD_MCP_ENV}} (lines 74-76). | Rename to 'local mode' and note that container-vs-workspace paths and per-mode vars must be branched with {{#if DEPLOYMENT_MODE_IS_DOCKER}} in mcp-json.tpl. |
| 9 | low | stale | "The default image is alpine:3.20" _(Alpine musl vs. glibc, line 42)_ | docker/Dockerfile:5 `ARG BASE_IMAGE=alpine:3.24.1`; the wizard records resolve-and-record latest stable (setup.sh:1060), so scaffolds pin the current alpine, not 3.20. | Say the base is Alpine with the version resolved at scaffold time and recorded in agent.yml docker.base_image (Dockerfile fallback currently 3.24.1). |
| 10 | low | needs-qualifier | Workspace is the "bind-mount of ~/agents/<name>/" (and cd ~/agents/<name> in examples) _(File Paths Inside the Container line 20 + lines 105, 145)_ | The default destination is <parent-of-installer>/agents/<agent_name> (setup.sh:534-538), and it is user-overridable via --destination; ~/agents/<name> only holds if the launcher clone lives directly under $HOME. | Refer to 'the workspace directory chosen at scaffold time' instead of the literal ~/agents/<name> path. |
| 11 | low | needs-qualifier | Check logs with `docker exec <name> tail -f /workspace/claude.log` (no -u agent) and `docker exec <name> find /home/agent ...` _(Building and Testing line 154 + line 187)_ | The privilege model (cap_drop: ALL, docker-compose.yml.tpl:27-28) strips root of CAP_DAC_OVERRIDE/CAP_DAC_READ_SEARCH, so root execs can only read world-readable agent files; project convention (CLAUDE.md) is that every docker exec passes -u agent, and the supported path today is `./scripts/agentctl logs -f`. I did not run a live container to confirm which files are world-readable. | Use `./scripts/agentctl logs -f` (or add -u agent to the exec examples). |
| 12 | low | needs-qualifier | Example MCP entry uses `"command": "python -m mcp.servers.my_service"` (args embedded in the command string) _(Multi-Instance MCPs, line 130)_ | Every real entry in modules/mcp-json.tpl (lines 3-77) uses command = bare executable plus a separate args array; the .mcp.json contract treats command as the binary to exec, and no code path in this repo shell-splits it. | Split the example into "command": "python", "args": ["-m", "mcp.servers.my_service"]. |

**Coverage gaps**:
- MCP catalog (modules/mcps/<id>.yml declarative descriptors + scripts/lib/mcp-catalog.sh): the actual mechanism for adding a wizard-visible MCP — descriptor schema (id/spec/type/runtime/secret_env_var/secret_doc_url/post_install_note), auto-prompting from setup.sh, MCPS_<ID>_ENABLED render gating, mcps.defaults persistence, and the fact that descriptors ARE baked into the image at /opt/agent-admin/modules/mcps/
- Deployment modes (011): nothing on deployment.mode docker|local, per-mode branching in mcp-json.tpl ({{#if DEPLOYMENT_MODE_IS_DOCKER}} container paths vs {{DEPLOYMENT_WORKSPACE}}), or that an MCP added for docker mode needs a local-mode path/command decision
- Per-mode render variables an MCP author must know: {{GCAL_CREDS_PATH}}, {{VAULT_MCP_PATH}}, and the qmd wrapper contract from 016/T036 ({{QMD_MCP_COMMAND}} with args: [] + {{QMD_MCP_ENV}}, docker wrapper /opt/agent-admin/scripts/qmd-mcp vs rendered local agent-qmd-mcp.sh) — the canonical pattern for MCPs whose command differs per mode
- Real musl native-module recipe replacing the fictional Debian variant: 016 QMD_NATIVE_TOOLCHAIN build-arg toolchain + managed bun-install prefix with trustedDependencies, 017 compile-for-musl at image build (build-sqlite-vec.sh + _qmd_swap_sqlite_vec)
- Test touchpoints when adding an MCP: tests/helper.bash::wizard_answers (line 107), the hand-rolled answers array in tests/e2e-smoke.bats (line 53, deployment mode is the FIRST answer since 011), known_external in tests/schema.bats (line 62), plus tests/mcp-catalog.bats and tests/mcp-json.bats
- The --regenerate flow: `./setup.sh --regenerate` is what re-renders .mcp.json from agent.yml after a template or config change (setup.sh:2184) — the doc never mentions it and instead prescribes docker compose build
- Secrets-in-.env contract with the backup model: agent.yml is replicated in plaintext to the fork's backup/config branch, so MCP tokens must only ever live in .env (which travels encrypted as .env.age in backup/identity)

## docs/adding-a-notifier.md

_The doc is fundamentally out of sync with the code: it documents a sourced-function notifier design (notify_<channel>() with $1=message, silent exit) while the real contract — already in place in the shipped notifiers — is a standalone executable receiving run_id/status as args and the message on stdin, always exiting 0 and emitting the JSON envelope {channel, ok, latency_ms, error} on stdout, with non-JSON output flagged as an error by heartbeat.sh. Following the doc verbatim produces a notifier that never fires and a channel value that fails both schema validation (scripts/lib/schema.sh enum) and heartbeatctl set-notifier (_v_notifier allowlist), neither of which the doc mentions. Steps 3 (env-example.tpl block, regenerate() export) and the general wizard mechanism remain accurate; the test example and wizard prompt label are stale._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | Step 1: create a driver that 'implements a function notify_<channel>() taking $1 as the message', with an example that silently returns 0 and emits nothing _(Section '1. Create the driver' (lines 5-19))_ | scripts/heartbeat/heartbeat.sh:96-107 (invoke_notifier): notifiers are standalone scripts executed as `bash notifiers/${NOTIFY_CHANNEL}.sh "$run_id" "$status"` with the message piped on STDIN ($1 is run_id, $2 is status, NOT the message); output is validated with `jq empty` — no/non-JSON stdout is recorded as '{"channel":"error",...,"error":"notifier emitted non-JSON"}'. All three real drivers confirm: none.sh:4-5 (drains stdin, prints envelope), log.sh:6-7,12,36 (RUN_ID/STATUS args, msg=$(cat), printf JSON envelope, exit 0), telegram.sh:6-7,13,51. Also heartbeat.sh:99 falls back to none.sh if the file is not executable (`[ -x "$path" ]`). A notifier written per the doc defines a function that is never called, never sees the message, and produces an error envelope on every tick. | Rewrite step 1 to the real contract: an executable script that receives run_id ($1) and status ($2), reads the message from stdin, always exits 0, and prints a one-line JSON envelope {channel, ok, latency_ms, error} on stdout (model it on scripts/heartbeat/notifiers/log.sh). |
| 2 | high | stale | Implicit claim that steps 1-5 are the complete procedure to add a channel (wizard + env template + test) _(Whole doc (steps 1-5))_ | scripts/lib/schema.sh:53 hard-codes the enum '.notifications.channel=none,log,telegram' in _SCHEMA_ENUMS, and setup.sh:11 sources schema.sh with agent_yml_validate wired in at setup.sh:2498 — an agent.yml with `channel: discord` fails schema validation, so the documented procedure yields a workspace that breaks on validation/regenerate. | Add a step: extend the .notifications.channel enum in scripts/lib/schema.sh (_SCHEMA_ENUMS) with the new channel name. |
| 3 | medium | stale | No mention that heartbeatctl must also learn the new channel _(Whole doc (missing step between 2 and 3))_ | docker/scripts/heartbeatctl:515-518 (_v_notifier) allowlists only `none\|log\|telegram`, so `heartbeatctl set-notifier discord` is rejected ('invalid notifier'); heartbeatctl:184,207 renders NOTIFY_CHANNEL into heartbeat.conf on reload. heartbeatctl is image-baked at /opt/agent-admin (docker mode) — changing the validator requires an image rebuild, unlike the notifier script itself which is workspace-templated/bind-mounted. | Add a step: extend _v_notifier in docker/scripts/heartbeatctl (image rebuild required in docker mode) so heartbeatctl set-notifier accepts the new channel. |
| 4 | medium | stale | Step 4 test example: `source .../discord.sh; run notify_discord "hello"; [ $status -eq 0 ]` _(Section '4. Add a test' (lines 44-55))_ | tests/notifiers.bats:8-16 tests the real contract: `run bash "$REPO_ROOT/scripts/heartbeat/notifiers/none.sh" "run-1" "ok" <<<"hello"` then asserts each JSON envelope field with jq (.channel/.ok/.latency_ms/.error). No test in the suite sources a notifier or calls a notify_* function; the doc's example would pass trivially while exercising nothing the runtime uses. | Replace the example with the actual pattern: run the script with run_id/status args and the message on stdin, then assert exit 0 and the JSON envelope fields with jq (copy the none.sh test in tests/notifiers.bats). |
| 5 | low | stale | Step 2 snippet: `notify_channel=$(ask_choice "Notification channel" "none" "none log telegram discord")` as the single wizard registration point _(Section '2. Register in the wizard' (lines 21-29))_ | setup.sh:626 — actual prompt label is "Heartbeat notification channel"; the same choice list is repeated in the review/edit menu at setup.sh:1027, the summary line at setup.sh:905, and the channel help text at setup.sh:615-619, none of which the doc mentions. | Update the prompt label to 'Heartbeat notification channel' and note the three additional wizard touchpoints (help text ~615, summary ~905, edit menu ~1027) that list channels. |

**Coverage gaps**:
- Deployment modes (feature 011): the doc predates deployment.mode and never says where notifiers fire per mode — the heartbeat scheduler is the container crond (docker mode); no local-mode heartbeat unit exists (no local-heartbeat*.tpl among modules/local-*.tpl and no heartbeat entry in scripts/lib/local_schedule.sh), so the doc should state the notifier flow's mode applicability
- Runtime channel switching: `heartbeatctl set-notifier <channel>` + `reload` is the post-scaffold path (agent.yml .notifications.channel -> modules/heartbeat-conf.tpl:13 NOTIFY_CHANNEL) — the doc only covers the scaffold-time wizard path
- Schema validation as a registration point: scripts/lib/schema.sh (relocated to scripts/lib in feature 012, mirrored to docker/scripts/lib) enforces the channel enum and must be extended for any new channel
- Docker vs workspace code split for this flow: notifier scripts are workspace-templated (bind-mounted, no rebuild), while heartbeatctl (validator) is image-baked at /opt/agent-admin — a new channel touches both sides

## docs/qmd-upgrade-checklist.md

_The doc's 016-era content is accurate and fully verified against current code: pin 2.5.3 (qmd_index.sh:56, qmd-version-guard.bats), trustedDependencies manifest (qmd_index.sh:150-151), node-llama-cpp build recipe (qmd_index.sh:207-208, bigstack at 99-100/248), toolchain packages (Dockerfile:66), and the DOCKER_E2E Tier 1 / QMD_EMBED_E2E Tier 2 gates (docker-e2e-qmd.bats:335,356). Drift is by omission: it predates 017 and 018, so it never mentions the sqlite-vec 0.1.9 pairing (second guard test tests/qmd-sqlite-vec.bats will fail any bump unexplained) nor 018's dependence on qmd's exact status/embed output strings and the 30-min session cap. Two findings (one stale, one needs-qualifier), no false claims._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | medium | stale | Before raising the pin, verify all of the following... then update `tests/qmd-version-guard.bats` to the new string (repeated as checklist item: '`tests/qmd-version-guard.bats` updated to assert the new version string') _(Intro paragraph (lines 5-7) and checklist item 5 (line 32))_ | Post-017 a second guardrail test also pins the qmd version: tests/qmd-sqlite-vec.bats:72-81 ('Dockerfile pins SQLITE_VEC_VERSION to the known-good 0.1.9 (paired with qmd 2.5.3)') asserts BOTH `qmd_pkg` = @tobilu/qmd@2.5.3 (line 78) AND `ARG SQLITE_VEC_VERSION=0.1.9` in docker/Dockerfile (line 79; Dockerfile:62). specs/017-qmd-sqlite-vec-musl/contracts/version-guardrail.md mandates asserting the PAIR so a qmd bump forces re-verifying the sqlite-vec musl compile. Following the doc verbatim updates only qmd-version-guard.bats and leaves an unexplained failure in qmd-sqlite-vec.bats, with no guidance on the sqlite-vec re-verification the pair exists to force. | Extend the item to also update tests/qmd-sqlite-vec.bats and re-verify the qmd<->sqlite-vec pair (transitive sqlite-vec version vs Dockerfile ARG SQLITE_VEC_VERSION). |
| 2 | low | needs-qualifier | In qmd 2.6.x the tree-sitter-* packages moved from optionalDependencies to hard dependencies _(Section 'Why this exists', third bullet (lines 16-18))_ | Ran `npm view @tobilu/qmd versions --json` (2026-07-12): latest published version is 2.5.3 — no 2.6.x exists on the npm registry. The repo's own 016 contract acknowledges this: specs/016-qmd-native-deps/contracts/qmd-version-guardrail.md:22 'hoy no existe 2.6.x estable... el guardrail es prevención, no un fix presente'. Stated as fact about a released series, it can mislead a reader into believing a hard-dep 2.6.x is installable today. | Qualify that 2.6.x is unpublished/upstream (observed in the repo, not on npm) and the guardrail is preventive. |

**Coverage gaps**:
- 017 sqlite-vec pairing: no checklist item to verify the target qmd's transitive sqlite-vec version still matches the baked Dockerfile ARG SQLITE_VEC_VERSION=0.1.9 (docker/Dockerfile:62,68), that the musl amalgamation compile (docker/scripts/build-sqlite-vec.sh, -Du_int8_t shim) still applies, and that _qmd_swap_sqlite_vec's target path node_modules/sqlite-vec-linux-arm64/vec0.so (scripts/lib/qmd_index.sh:128-141) still matches the package layout — the doc never mentions sqlite-vec at all
- 018 CLI output contract: the embed-completion loop parses qmd's output verbatim — _qmd_pending_count greps `Pending: N` from `qmd status` (scripts/lib/qmd_index.sh:470-473) and completion is detected via 'All content hashes already have embeddings' (qmd_index.sh:498); a bump that changes these strings silently degrades the loop to the QMD_EMBED_MAX_PASSES cap with last_status=partial — the checklist should verify both strings on the target version
- 018 session-cap rationale: the multi-pass loop exists because qmd embed hardcodes a 30-min session cap (specs/018-qmd-embed-completion/spec.md); a new version may change/remove the cap, so the checklist should re-check it and whether QMD_EMBED_MAX_PASSES=12 (qmd_index.sh:463) still bounds a full-corpus embed
- 013 storage env contract: the reindex/MCP paths depend on the qmd binary honoring XDG_CACHE_HOME + QMD_CONFIG_DIR (QMD_CACHE_HOME is launcher-side only, qmd_index.sh:62,454-456); a bump changing storage resolution would orphan the existing index under .state — worth a verification line
- 016 T036 MCP invocation: the MCP server is launched as `qmd ... mcp` from the managed prefix via qmd_mcp_exec (qmd_index.sh:256+); the checklist should confirm the `mcp` subcommand survives the bump
- References section lists only 016 artifacts; specs/017-qmd-sqlite-vec-musl/contracts/version-guardrail.md and specs/018-qmd-embed-completion/contracts/{embed-completion,reindex-state}.md are now equally load-bearing for a bump

## modules/next-steps.en.tpl

_The template is in good shape overall: both mode branches exist ({{#if DEPLOYMENT_MODE_IS_DOCKER}}), and the local-mode content (login flow, unit names, healthcheck cadence, min Claude 2.1.51, RestartSec=10, CLAUDE_CONFIG_DIR under .state) verifies exactly against the current templates and setup.sh. The one high-severity defect is a security/restore claim inverted from reality — it says partial-mode identity backup pushes .env in plaintext to the fork, when the code omits .env entirely without a recipient — plus two wrong agentctl command semantics (stop is `compose down`, restart is `compose restart`, not stop+up) and a stale doctor check count. The largest structural drift is asymmetry: the docker branch never gained the vault/QMD/wiki-graph next-steps sections that 012-014 made mode-agnostic, and `agentctl versions` and local `doctor` are undocumented._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | With identity backup in partial mode (no SSH recipient) the .env is backed up in plaintext to the fork, so prefer a configured recipient when using a token _(Docker branch, section '2. Authenticate Claude — Headless token (recommended)', lines 39-41)_ | docker/scripts/lib/backup_identity.sh:152-158 — .env is staged ONLY as age-encrypted .env.age when a recipient exists; the else branch runs `rm -f "$stage/.env.age"` and never stages plaintext .env. A grep of every .env reference in that lib (lines 40-77, 152-157) shows .env is otherwise used only for the idempotency hash. In partial mode the token is not in the fork at all — neither plaintext nor encrypted. | Invert the claim: in partial mode .env is omitted from the identity backup entirely (the token is NOT recoverable via --restore-from-fork), so configure an SSH recipient to get the token backed up encrypted as .env.age. |
| 2 | medium | false | ./scripts/agentctl stop  # docker compose stop (state preserved) _(Docker branch, 'Full agentctl cheatsheet', line 112)_ | scripts/agentctl:768 `cmd_stop() { cmd_compose down; }` and its own usage text at scripts/agentctl:631 'stop → docker compose down'. `down` removes the container (compose ps empty, docker logs lost); state survives only because it lives in the .state/ bind-mount. | Change the comment to 'docker compose down (container removed; state under .state/ preserved)'. |
| 3 | medium | false | ./scripts/agentctl restart  # stop + up _(Docker branch, 'Full agentctl cheatsheet', line 113)_ | scripts/agentctl:769 `cmd_restart() { cmd_compose restart; }` (usage text scripts/agentctl:632 'docker compose restart') — restarts the existing container in place; it does NOT recreate it, so compose-file/regenerate changes are not applied, unlike a real stop+up. | Describe restart as 'docker compose restart (in-place; compose-file changes need stop + up)'. |
| 4 | low | stale | doctor runs '12 dependency-ordered checks' / 'Runs 12 checks in dependency order (Docker daemon → … → vault → patches)' _(Docker branch, cheatsheet line 127 and Troubleshooting 'run agentctl doctor first', line 168)_ | scripts/agentctl cmd_doctor (lines 328-~620): numbered checks now include 1-12 plus 5b (launcher version stamp), 5c (recorded image toolchain), 12b (.state bind-mount writable), 13-15 (identity/vault/config backup freshness), 15b (plugin install failures), 16-18 (token health) — roughly 22 checks, verified via grep of '# N.' comments inside the function. | Drop the hardcoded '12' (say 'a battery of dependency-ordered checks') or update the count and the arrow list to include versions, .state mount, backup freshness and token health. |
| 5 | low | stale | Subcommands: attach, logs [-f], status, heartbeat <sub>, mcp [list], shell [--root], up, stop, restart, ps, run <cmd…> _(Docker branch, note under '1. Build and launch', line 19)_ | scripts/agentctl usage (lines 626, 645) and dispatch (lines 1243, 1251) also implement `doctor` and `versions [--check\|--json\|--upgrade]`; the enumeration presents itself as complete but omits both. | Add `doctor` and `versions` to the enumerated subcommand list. |
| 6 | low | stale | ./scripts/local/agent-killswitch.sh  # KILL SWITCH (stops session + qmd + backup + healthcheck) _(Local branch, section '2. Operation', line 304)_ | modules/local-killswitch.sh.tpl:18-20 — AUX_UNITS includes agent-<name>-wiki-graph.timer (added with 014) in addition to qmd-reindex.timer, qmd-watch.service, vault-backup.timer and healthcheck.timer. | Add wiki-graph to the parenthetical list of what the kill switch stops. |
| 7 | low | needs-qualifier | ./setup.sh --login  # verifies the version, pre-seeds onboarding, launches OAuth, applies workspace trust, and enables the systemd session _(Local branch, section '1. Full-scope login', lines 289-291)_ | modules/local-login.sh.tpl:78-81 — the login script also invokes agent-bootstrap.sh ('Provisioning MCP runtimes'), which installs uv/uvx, node/npx symlinks, bun and github-mcp-server into ~/.local/bin (modules/local-bootstrap.sh.tpl:1-32); without this step every MCP fails with ENOENT per remote-control.env.tpl comments. | Add 'provisions the MCP runtimes (agent-bootstrap.sh)' to the comment so users understand why --login needs network and where the binaries land. |

**Coverage gaps**:
- Docker branch has NO vault/QMD/RAG/wiki-graph section at all, while the local branch has three gated ones — yet docker heartbeatctl supports qmd-reindex and wiki-graph (docker/scripts/heartbeatctl:1056-1057) and docker mode renders the vault/qmd MCPs; a docker-mode user gets zero RAG next steps (features 010/012/013/014 parity).
- The docker 'heartbeat' cheatsheet omits `heartbeat qmd-reindex` and `heartbeat wiki-graph` (both dispatchable per docker/scripts/heartbeatctl:1056-1057).
- `agentctl versions [--check|--upgrade]` (feature 010 toolchain-pin management; scripts/agentctl:645, 857) is absent from the entire doc.
- Local branch never mentions `agentctl doctor` even though 013 gave local mode its own doctor with the 0/1/2 exit contract (scripts/agentctl:1237 cmd_local_doctor) — the troubleshooting entry point exists only in the docker branch.
- The 'N MCP servers failed' troubleshooting list ('plugin:telegram:telegram, atlassian-*, github, playwright') omits the `qmd` and `vault` MCP entries rendered into .mcp.json (modules/mcp-json.tpl:67-77), notable since 016 gave qmd its own wrapper command ({{QMD_MCP_COMMAND}}).
- No mention of the qmd embed completion state from 018 (qmd-index.json pending / last_status partial|stalled) as an RAG-freshness signal in the local 'RAG (QMD) — freshness & control' section.

## modules/next-steps.es.tpl

_La plantilla ES está línea-a-línea en paridad con la EN (344/344; variables idénticas) y toda la sección de modo local verificó limpia contra el código (unidades systemd, killswitch, healthcheck 5min/Restart=10s, MIN_VERSION 2.1.51, identidad hostname-agente, qmd-reindex/backup-vault --dry-run/wiki-graph). El drift real está en el cheatsheet docker: las anotaciones de `stop`/`restart` no reflejan la implementación de agentctl (down y compose-restart), lo que rompe silenciosamente las recetas de upgrade de imagen y rotación del token OAuth; además el conteo \"12 chequeos\" del doctor quedó corto y la lista de subcomandos omite doctor/versions. Única ruptura de paridad EN/ES: la línea 10 (ES adelantada con `agentctl up`); nota cosmética: \"corré\" (línea 160) es voseo, fuera del registro neutro del resto del doc._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | false | Receta de upgrade: `docker compose build && ./scripts/agentctl restart` y cheatsheet `restart # stop + up` _(Uso diario (líneas 101-104) + cheatsheet (línea 113))_ | scripts/agentctl:769 `cmd_restart() { cmd_compose restart; }` — es `docker compose restart`, NO stop+up. `docker compose restart` reinicia el contenedor existente sin recrearlo: no carga una imagen recién construida ni re-inyecta env_file. El token OAuth se consume desde el env del contenedor vía env_file (docker/scripts/start_services.sh:455-456 "delivered via docker-compose env_file"; modules/docker-compose.yml.tpl:67), fijado al momento de creación. | Cambiar ambas recetas a `docker compose up -d` (recrea el contenedor cuando la imagen o el env cambian) o corregir la anotación a `docker compose restart` y advertir que build/rotación de CLAUDE_CODE_OAUTH_TOKEN requieren `up -d --force-recreate`. |
| 2 | medium | false | `./scripts/agentctl stop # docker compose stop (preserva state)` _(Cheatsheet de agentctl (línea 112))_ | scripts/agentctl:768 `cmd_stop() { cmd_compose down; }` y el propio help del script (scripts/agentctl:631 `stop → docker compose down`). `down` REMUEVE el contenedor (el estado sí se preserva porque .state/ es bind-mount), no lo deja detenido. | Corregir la anotación a `docker compose down (el contenedor se remueve; el estado en .state/ se preserva)`. |
| 3 | low | stale | `agentctl doctor` hace "12 chequeos en orden de dependencia" (aparece dos veces) _(Cheatsheet (línea 127) y sección Troubleshooting (línea 168))_ | scripts/agentctl cmd_doctor: los checks numerados 1-12 existen, pero después corren checks adicionales — `_check_backup_freshness` x3 (identity/vault/config), `_doctor_check_plugin_failures` y `_check_token_health` para github/telegram/atlassian/claude_oauth (offsets 238-275 dentro de cmd_doctor). El total real supera 12. | Quitar el número fijo ("chequeos en orden de dependencia") o actualizar la lista agregando .env, frescura de backups, fallos de plugins y salud de tokens. |
| 4 | low | stale | Lista de subcomandos de agentctl: `attach, logs, status, heartbeat, mcp, shell, up, stop, restart, ps, run` _(Nota tras el primer attach (línea 19))_ | scripts/agentctl dispatch: `doctor` (línea 1243) y `versions` (línea 1251) existen y están en el help propio (líneas 626 y 645); la lista del doc los omite aunque el mismo doc documenta doctor más abajo. | Agregar `doctor` y `versions [--check]` a la lista de subcomandos. |
| 5 | low | needs-qualifier | "La sesión vive en el UID de `agent` (501 por default)" _(Troubleshooting → no sessions, causa 1 (línea 213))_ | docker/Dockerfile:8 `ARG UID=1000` es el default real de la imagen; setup.sh hornea el UID detectado del host como build-arg. 501 es solo el UID típico de macOS, no un default del sistema. | Reformular a "el UID del host horneado en el build (típicamente 501 en macOS, 1000 en Linux)". |
| 6 | low | needs-qualifier | Paso 1 usa `./scripts/agentctl up` mientras la plantilla EN usa `docker compose up -d` (ruptura de paridad EN/ES) _(Sección 1 Build y arranque (línea 10))_ | diff de bloques de código EN vs ES: única divergencia de comando. modules/next-steps.en.tpl:10 = `docker compose up -d`; modules/next-steps.es.tpl:10 = `./scripts/agentctl up`. La versión ES es la correcta/mejor: cmd_up pre-crea .state/ si falta (scripts/agentctl:754-767, guard del bind-mount roto en macOS); EN quedó atrás. Además el propio ES sigue usando `docker compose up -d` crudo en el bloque de token headless (línea 36), sin ese guard. | Portar `./scripts/agentctl up` a next-steps.en.tpl:10 y considerar usarlo también en el bloque de token headless de ambas plantillas. |

**Coverage gaps**:
- Rama docker sin operaciones RAG (010/014): `heartbeatctl qmd-reindex` y `wiki-graph` existen en modo docker (docker/scripts/heartbeatctl:1056-1057) pero el cheatsheet y el troubleshooting docker no los mencionan, mientras la rama local sí documenta ambos.
- Rama local sin `agentctl doctor`: el doctor local con contrato exit 0/1/2 existe desde 013 (cmd_local_doctor, scripts/agentctl:1131, con checks de qmd-watch/vault-backup/wiki-graph) pero la sección local nunca lo menciona, a diferencia de la rama docker que lo recomienda como primer paso.
- 018: la sección RAG local no advierte que un primer embed grande corre en múltiples pasadas y cómo verificar completitud (campo `pending` y estados `partial`/`stalled` en qmd-index.json) — el operador que ve hits flojos no tiene señal de corpus parcial.

## modules/claude-md.tpl

_The template got a mode-aware Identity/Runtime paragraph in 011, but every operational section it teaches the agent — Heartbeat, Permission Mode, and the Memory path table — is still hardcoded to the docker runtime (/home/agent paths, crond, tmux kick-channel, ~/.vault), so a local-mode agent receives a CLAUDE.md that is wrong about its own scheduler, config location, vault location, and restart procedure. Two claims are wrong even in docker: the advice to hand-edit heartbeat.conf + docker compose restart contradicts the conf's own do-not-edit header and is silently reverted by the boot-time heartbeatctl reload. Nothing shipped in 010-018 (qmd search MCP, self-reindexing RAG, wiki-graph, the three backup branches) is taught to the agent._

| # | Sev | Verdict | Claim (doc) | Evidence (code) | Fix |
|---|-----|---------|-------------|-----------------|-----|
| 1 | high | needs-qualifier | Heartbeat: "Periodic execution system... Runs automatically inside the container via crond" — rendered whenever FEATURES_HEARTBEAT_ENABLED, in both modes _(## Heartbeat, line 81)_ | The wizard asks the heartbeat question ungated by mode (setup.sh:798) and heartbeat.conf renders in both modes (setup.sh:2208), but NO local scheduler exists: modules/ has local units only for healthcheck/qmd-reindex/qmd-watch/vault-backup/wiki-graph (ls modules/); grep for heartbeat.sh across modules/ and scripts/local matched only claude-md.tpl itself; docs/architecture.md:19 states heartbeat scheduling was 'not ported to systemd in v1'; agentctl's local dispatch only accepts qmd-reindex\|backup-vault\|wiki-graph and degrades everything else (scripts/agentctl:1193-1222). | Gate the crond/container wording on DEPLOYMENT_MODE_IS_DOCKER and, in local mode, state that the heartbeat is NOT scheduled automatically (no systemd timer exists for it). |
| 2 | high | false | "To inspect or change the heartbeat behavior, edit scripts/heartbeat/heartbeat.conf (interval, prompt, retries, timeout) and restart the container with docker compose restart" _(## Heartbeat, line 89)_ | modules/heartbeat-conf.tpl:1-3 header: 'Generated by setup.sh / heartbeatctl reload. Do not edit by hand — use heartbeatctl set-* (writes agent.yml + regenerates this file)'. Boot runs 'heartbeatctl reload' (docker/scripts/start_services.sh:138) which regenerates the conf from agent.yml — so an edit + restart is silently reverted; interval changes propagate via heartbeatctl set-interval → crontab staging + root sync loop within ~1 min, no restart needed (docker/entrypoint.sh:66-70 comment; heartbeatctl:1059, cmd_reload at 173). | Replace with 'use heartbeatctl set-interval/set-prompt/set-timeout/set-retries (writes agent.yml, regenerates the conf and crontab; takes effect within ~1 minute, no restart)'. |
| 3 | high | needs-qualifier | "You run with a permission mode set in /home/agent/.claude/settings.json... The interactive session defaults to auto" (and the jq snippet targeting /home/agent/.claude/settings.json) _(## Permission Mode, lines 143 and 152-155)_ | Docker-only: settings="$HOME/.claude/settings.json" with HOME=/home/agent and defaultMode=auto is written by docker/scripts/start_services.sh:479-498; in local mode CLAUDE_CONFIG_DIR={{DEPLOYMENT_WORKSPACE}}/.state/.claude and HOME={{OPERATOR_HOME}} (modules/remote-control.env.tpl:6-8), no code writes defaultMode=auto (repo-wide grep for defaultMode matched only start_services.sh and this template), and modules/systemd-remote-control.service.tpl explicitly keeps permission prompts enabled ('the dangerous skip flag is intentionally absent'). | Render the settings path per mode (/home/agent/.claude/settings.json vs <workspace>/.state/.claude/settings.json) and qualify that the auto default is docker-mode behavior — local mode intentionally keeps prompts on. |
| 4 | high | needs-qualifier | Memory table: Vault at "~/.vault/ (alias ~/vault)" and "See ~/.vault/CLAUDE.md" _(## Memory table, lines 131 and 139)_ | True in docker (docker/scripts/start_services.sh:84 vault_root=/home/agent/.vault; :126 ln -s → /home/agent/vault). False in local mode: vault resolves to LOCAL_VAULT_DIR=$SCRIPT_DIR/.state/.vault (setup.sh:1981), _seed_vault_local's comment says it 'creates no /home/agent symlink' (setup.sh:2291-2294), and HOME is the operator's real home (remote-control.env.tpl:8) — so ~/.vault does not exist and a local agent following this could create a stray vault in the operator's home. | Render the vault path per mode: ~/.vault (docker) vs <workspace>/.state/.vault (local), including the CLAUDE.md pointer. |
| 5 | medium | needs-qualifier | Permission-mode restart procedure: run "heartbeatctl kick-channel"; "the session comes back in ~2 seconds" _(## Permission Mode, step 3, lines 158-164)_ | kick-channel exists but only image-baked in docker (docker/scripts/heartbeatctl:1051; installed at /usr/local/bin inside the container). In local mode there is no heartbeatctl binary and no tmux/watchdog — the session is the systemd remote-control unit (modules/systemd-remote-control.service.tpl ExecStart=claude remote-control, Restart=always), and agentctl's local heartbeat proxy supports only qmd-reindex\|backup-vault\|wiki-graph (scripts/agentctl:1196-1222). | Gate step 3 per mode: heartbeatctl kick-channel (docker) vs restarting the agent-<name> systemd remote-control unit (local). |
| 6 | medium | needs-qualifier | Auto-memoria layer at "~/.claude/projects/-workspace/memory/" _(## Memory table, line 129)_ | Correct for docker (cwd /workspace, HOME=/home/agent). In local mode the working directory is the workspace path (modules/systemd-remote-control.service.tpl:11 WorkingDirectory={{DEPLOYMENT_WORKSPACE}}) and the config dir is <workspace>/.state/.claude (remote-control.env.tpl:6), so the memory dir is <ws>/.state/.claude/projects/<slugified-workspace-path>/memory/ — and ~/.claude in local mode is the OPERATOR's personal config dir, which the doc would direct the agent to write into. | Render the auto-memory path per mode (or describe it as '<config dir>/projects/<project slug>/memory/' instead of hardcoding the docker slug). |
| 7 | medium | needs-qualifier | Telegram Integration section (ack-first protocol, typing indicator, reply behavior) is gated on NOTIFICATIONS_CHANNEL_IS_TELEGRAM _(## Telegram Integration, lines 92-108 ({{#if NOTIFICATIONS_CHANNEL_IS_TELEGRAM}}))_ | NOTIFICATIONS_CHANNEL_IS_TELEGRAM derives from notifications.channel (setup.sh:2132-2134), which the wizard explicitly scopes to one-way heartbeat pings: 'NOTE: This is ONLY for heartbeat pings. For two-way Telegram chat with your agent, install the plugin separately' (setup.sh:621-623). The section's content (acks, mid-progress replies, typing indicator) describes the two-way chat plugin — so agents with the chat plugin but notify=none/log never receive this guidance, and notify=telegram agents without the plugin are taught reply behavior they cannot perform. | Split the section: keep the NOTIFY_BOT_TOKEN/NOTIFY_CHAT_ID lines under the notifier gate, and gate the ack/typing protocol on the telegram chat plugin being installed (or make it unconditional chat guidance). |
| 8 | low | stale | "Backend: busybox crond inside the container, configured from docker/crontab.tpl" _(## Heartbeat, line 87)_ | Even in docker, crontab.tpl only supplies the boot-time safe default (docker/entrypoint.sh:55-58: envsubst with HEARTBEAT_CRON defaulting to */30); the effective crontab is generated by heartbeatctl reload from agent.yml (start_services.sh:138 at boot; heartbeatctl cmd_reload builds the full multi-line crontab with backup/qmd/wiki-graph lines, heartbeatctl:199-305) and synced from the staging file. | Say the schedule is managed by heartbeatctl reload from agent.yml (docker/crontab.tpl is only the boot fallback). |

**Coverage gaps**:
- QMD hybrid/semantic search (features 010/013/016-018): when vault.qmd is enabled the agent gets a qmd MCP server over its vault plus a self-managing reindex pipeline (watcher + qmd-reindex schedule, 018 multi-pass embed), but neither this template nor the vault-skeleton CLAUDE.md mentions qmd at all (grep -ri qmd modules/claude-md.tpl modules/vault-skeleton/ → no matches) — the agent is never told its main retrieval capability exists.
- Backup expectations (hint topic): nothing about the three orphan-branch backups (identity/vault/config), their schedules, or how the agent can trigger/inspect them (heartbeatctl backup-identity/backup-vault/backup-config in docker, agentctl heartbeat backup-vault in local) — the agent cannot reason about the durability of its own state or vault.
- heartbeatctl self-service surface: the supported mutation/inspection path (set-interval, set-prompt, set-notifier, pause/resume, status, logs) is absent — the doc instead teaches editing the derived heartbeat.conf, the exact anti-pattern the conf header forbids.
- Local-mode operational guidance (011-013): the Identity/Runtime paragraph is mode-aware, but there is no local-mode counterpart for any operational instruction — where config/state actually live (<ws>/.state/.claude), how the agent is restarted (systemd remote-control unit), or which agentctl subcommands work locally (status/doctor/heartbeat subset).
- Wiki-graph (014), minor: the Memory section never hints that <vault>/.graph/{graph,backlinks,findings}.json exist or that a scheduled runner maintains them — it is covered only inside the vault's own CLAUDE.md, which a local agent may not even find given the ~/.vault path bug above.
